# 本地知识库 Agent 项目技术介绍演讲稿

配套展示文件：`本地知识库Agent介绍.html`  
建议时长：约 10 分钟  
表达原则：先讲痛点，再讲作品实现的功能、流程架构、核心技术和业务收益。答辩时不要平均用力，重点讲“解决了什么问题、怎么实现、比原流程好在哪里”。

## 10 分钟答辩主线

开场 30 秒：本项目做的是本地知识库 Agent，把企业内部 Word、PDF、Excel、扫描件等资料变成可检索、可引用、可复核的可信答案。

痛点 1 分钟：原流程主要靠人工翻资料、问老员工、查 Excel 和 PDF。问题是资料分散、版本难确认、答案没有证据、复杂表格和扫描件容易漏看，新人学习和跨部门沟通成本都高。

功能 2 分钟：作品实现了多格式资料入库、OCR 辅助抽取、语义分块、本地索引、混合检索、结构化业务 Retriever、答案引用校验、健康检查、增量更新和 Web/命令行问答。

流程架构 2 分钟：整体流程是“资料入库 -> 文本抽取 -> 质量报告 -> 语义分块 -> 建立 FTS5 和本地向量索引 -> 用户提问 -> 领域路由 -> 混合检索 -> 重排 -> 证据约束生成答案”。

技术 3 分钟：重点讲 SQLite 本地库、FTS5 全文检索、本地 hash n-gram 384 维 embedding、LSH 候选筛选、rerank 重排、OCR、结构化 Excel/站位 Retriever、引用校验和异常降级。

收益 1 分钟：相比人工查资料，系统能更快定位证据，减少重复答疑，降低资料误用风险，让测试项、门限、流程和经验变成可持续复用的知识资产。



### 创新优化点：

rag召回率： 把分块策略从固定长度改成语义分块，索引从定时重建改成实时更新，加了混合检索。
召回率从40%提到85%。

检索升级：不再用传统的只用langchain做检索，而是热门Query走缓存，减少重复检索；检索模块做降级，向量数据库挂了走FAQ兜底；知识库异步更新，不阻塞用户请求

降级策略：当大模型服务挂了，怎么保证体验不下降，可以设计多级降级：
 本地小模型兜底
 缓存高频问题答案
 引导用户留言，稍后回复大模型崩了，用户体验也不会断崖。 这里设计了FAQ降级策略以及缓存答案。

## 第 1 页｜让本地资料变成可信答案

各位好，今天介绍的是本地知识库 Agent。它面向企业内部文档、测试计划、工艺流程和经验资料，核心目标不是做一个普通聊天工具，而是把本地资料转化为可检索、可引用、可复核的可信答案。

## 第 2 页｜项目痛点与价值

目前很多企业资料分散在 Excel、Word、PDF、扫描件和个人经验里。原流程通常是人工翻文件、搜索关键词、再找熟悉业务的人确认。这个过程慢、容易漏、版本不清楚，答案也很难证明来自哪份资料。本项目解决的痛点就是：资料难找、答案难核验、经验难沉淀。

## 第 3 页｜作品实现了什么功能

这个作品已经实现了完整的本地知识库问答闭环：支持多格式资料入库，支持 OCR 辅助识别，能自动分块、建索引、检索证据、生成答案，并返回来源引用。同时提供 Web UI、命令行、一键脚本、健康检查和质量报告，方便普通用户使用和维护。

## 第 4 页｜对比当前流程的优势

和传统人工查资料相比，本项目有四个优势：第一，查找方式从“打开多个文件翻内容”变成“自然语言提问”；第二，答案不是凭经验口述，而是带来源片段；第三，资料更新后可以重新建库或增量更新，降低旧版本误用；第四，常见问题和结构化表格可以被重复复用，减少专家重复答疑。

## 第 5 页｜系统架构

系统采用轻量本地 RAG 架构，分为入口层、Agent 编排层、检索与证据层、知识治理层、模型与存储层。底层以 SQLite 为主，不依赖独立向量数据库；没有 API 时可以本地检索，有 API 时可以进一步做答案精炼。

## 第 6 页｜正式分层架构

这套架构的重点是本地可运行、证据可追溯、状态可检查。纵向链路负责从文件入库到问答输出；横向治理负责缓存、日志脱敏、异常降级、互斥锁、健康检查和质量报告。也就是说，主流程负责回答问题，治理能力负责让系统稳定、可信、可维护。

## 第 7 页｜知识入库

知识入库不是简单读取文件，而是一条治理流水线。系统先抽取 `.md`、`.txt`、`.doc`、`.docx`、`.pdf`、`.xlsx` 等资料内容，再生成抽取报告、语义分块、全文索引、本地向量和质量标记。复杂版式、图表、OCR 风险不会被静默忽略，而是进入复核提示。

## 第 8 页｜入库流程

入库时，每份文档会被标准化为纯文本，再按标题、段落、表格行和语义边界切成 chunk。默认最大块长约 1100 字符，180 字符以上才允许因标题、长度或语义差异断块，避免产生过多碎片。每个 chunk 都会保留文件名、片段编号和引用内容，方便后续追溯。

## 第 9 页｜问答流程

用户提问后，系统先检查本地索引状态和缓存，再判断问题类型。如果是缩写、站位、测试项或 Excel 字段问题，优先走结构化 Retriever；否则进入通用混合检索。系统不是先让大模型自由回答，而是先找证据，再根据证据生成答案。

## 第 10 页｜混合检索

检索不是只靠关键词，也不是只靠向量。系统同时使用 FTS5 全文检索、本地 hash n-gram embedding 语义召回和 SQL LIKE 兜底召回，再统一进入 rerank。小规模数据直接扫描向量，大规模数据通过 LSH 候选桶缩小范围，提高检索效率。

## 第 11 页｜本地 Embedding

本项目没有调用外部 embedding 模型，而是使用本地 `local-hash-ngram-384d`。英文和数字提取 token，中文切 2、3、4 字 n-gram，每个 term 通过 blake2b 哈希映射到 384 维稀疏向量，最后做 L2 归一化。这样可以在本地完成轻量语义召回，部署成本低，也便于离线使用。

## 第 12 页｜重排逻辑

重排分数由四部分组成：semantic score 占 48%，keyword score 占 34%，FTS score 占 18%，再乘以 information quality score。它同时考虑语义相似、关键词覆盖、全文检索相关性和片段信息质量，避免目录、空表头、低信息片段排在真正答案前面。

## 第 13 页｜领域 Retriever

项目没有把所有问题都交给通用 RAG。它提供 General RAG、Abbreviation、Station Test、Excel Table 等 Retriever，用于处理缩写解释、站位测试项、测试门限和结构化表格字段查询。这样可以把通用问答和业务规则结合起来，提高表格类、测试类问题的准确度。

## 第 14 页｜工程可信

可信性来自三个约束：第一，答案必须带来源；第二，复杂版式、OCR 风险和低信息片段会被标记或降权；第三，答案生成后会校验引用，避免出现有答案但无依据的情况。答辩时可以强调：本项目重点不是“能说”，而是“有证据地说”。

## 第 15 页｜核心模块

核心模块包括 `ingest_knowledge_base.py`、`query_knowledge_base.py`、`retrievers/*`、`healthcheck_agent.py` 和 Web/命令行入口。入库、查询、领域检索、健康检查和用户操作被拆成清晰接口，后续替换 embedding、向量库或业务 Retriever 时，不需要推倒重来。

## 第 16 页｜用户操作闭环

普通用户不需要理解底层算法。首次使用只需初始化、放入资料、构建索引并检查状态；日常使用可以通过 Web UI 或命令行提问，查看答案、来源、索引状态和复核提示。这个闭环让作品不只是代码 Demo，而是可以被业务人员实际试用。

## 第 17 页｜N972 真实资料验证

项目使用 N972 装备测试计划进行验证。该资料约 4.8MB，包含 23 个工作表，覆盖 SMT、SWDL、FAT、FRT、FFT、MES、测试项、测试方案和门限等复杂业务内容。选择这类真实资料，是为了验证系统面对复杂 Excel 和测试计划时是否真的可用。

## 第 18 页｜入库质量报告

N972 隔离入库生成 981 个知识片段，平均片段长度约 856 字符，空片段和重复文档为 0。同时系统识别到 drawings/charts 风险，并标记 `requires_review`，说明图形化信息不会被静默当作可靠文本，而是会提示人工复核。

## 第 19 页｜验证题库

验证题覆盖精确事实、列表召回、字段定位和数值门限，例如文件编号、版本、RTC Test 门限、TYPE_C 子项、Battery Test 电量跳变 SPEC 等。这些问题对应真实业务查询场景，用来检查系统能不能找到正确来源并给出可核验答案。

## 第 20 页｜收益与价值

项目带来的收益主要有三点：第一，提高资料查询效率，减少人工翻找；第二，答案带证据，降低沟通争议和资料误用风险；第三，把专家经验、测试项、门限和流程沉淀成可复用资产。对企业来说，它能支持新人培训、测试问题定位、跨部门协作和知识治理。

## 第 21 页｜演进路线与总结

当前方案以 SQLite、FTS5 和本地 hash n-gram embedding 为轻量底座。后续可以升级到 BGE/E5 等神经网络 embedding，也可以迁移到 FAISS、Qdrant、Milvus 或 sqlite-vec。总结来说，本项目不是替代专家，而是让专家知识可沉淀、可复用、可追溯，让本地资料真正变成企业知识资产。谢谢大家。
