# The core command-line agent uses only the Python standard library.
# Optional packages can improve extraction/conversion quality when available.
pypdf>=4.0.0
PyPDF2>=3.0.0
opencc-python-reimplemented>=0.1.7
pdfplumber>=0.11.0
Pillow>=10.0.0
pytesseract>=0.3.10
openpyxl>=3.1.0
requests>=2.32.0
# Optional FAISS vector backend for API-provided dense embeddings. The app
# automatically falls back to SQLite + local hash embeddings when unavailable.
faiss-cpu>=1.14.3
numpy>=2.5.1
# Optional offline English-to-Chinese answer translation. Install the en -> zh
# Argos model after installing this package.
argostranslate>=1.11.0
