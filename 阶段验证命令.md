# 本地知识库 Agent 阶段验证命令

本文档用于记录每个阶段完成后的验证操作。后续阶段实现完成后，会继续把对应验证命令追加或更新到这里。

## 第一阶段：本地知识库 MVP

当前能力：

- 从 `knowledge_base/raw` 读取本地知识库文件
- 支持 `.md`、`.docx`、`.pdf`、`.xlsx`
- 提取文本并写入 SQLite FTS5 索引
- agent 查询本地知识库
- 输出 `answer`、`source_type`、`sources`

验证命令：

```bat
.\run-agent.bat ingest
```

```bat
.\run-agent.bat ask "加强针有什么作用？"
```

```bat
.\run-agent.bat ask "影响光合作用速率的因素有哪些？"
```

```bat
.\run-agent.bat ask "P waves 和 S waves 有什么区别？"
```

```bat
.\run-agent.bat ask "乙醇为什么 78 度开始沸腾？"
```

```bat
.\run-agent.bat ask "量子纠缠如何用于加密？"
```

预期结果：

- 前五条查询应返回 `source_type: "knowledge_base"`，并在 `sources` 中显示对应本地文件。
- 超出本地知识库范围的问题应返回 `source_type: "none"`，并提示本地知识库没有足够依据。

## 第二阶段：增加“答不了就询问联网”

当前能力：

- 查询结果增加 `need_web_search`
- 查询结果增加 `web_search_used`
- 不加 `--web` 时，只查本地知识库；本地答不了时提示可追加 `--web`
- 加 `--web` 时，本地答不了才联网搜索
- 联网搜索结果标识 `source_type: "web_search"`

验证命令：

```bat
.\run-agent.bat ask "加强针有什么作用？"
```

```bat
.\run-agent.bat ask "量子纠缠如何用于加密？"
```

```bat
.\run-agent.bat ask "量子纠缠如何用于加密？" --web
```

```bat
.\run-agent.bat ask "影响光合作用速率的因素有哪些？" --web
```

预期结果：

- 本地知识库能回答的问题应返回 `source_type: "knowledge_base"`、`need_web_search: false`、`web_search_used: false`。
- 本地知识库答不了且不加 `--web` 时，应返回 `source_type: "none"`、`need_web_search: true`、`web_search_used: false`，并提示追加 `--web`。
- 本地知识库答不了且加 `--web` 时，应返回 `source_type: "web_search"`、`need_web_search: false`、`web_search_used: true`。
- 本地知识库能回答的问题即使加了 `--web`，也应优先使用本地知识库，不联网。
- `answer`、`excerpt`、`snippet` 应是自然语言文本，不应包含 Markdown 标题符号、列表符号或换行残留，例如 `#`、`- `、`\n`。
- 联网搜索结果中的中文内容应输出为简体中文。

## 第三阶段：增强准确性

当前能力：

- 增加本地 embedding 语义检索，使用可复现的本地 hash n-gram 向量，不依赖外部 API
- 增加 chunk rerank，综合语义分、关键词分和 FTS 分排序
- 增加引用片段，输出 `citations` 和每个 source 的 `citation`
- 增加重复文件检测，导入时输出 `duplicates` 和 `duplicate_files`
- 增加索引更新时间，查询结果输出 `index_updated_at`
- 查询结果输出 `embedding_model`

验证命令：

```bat
.\run-agent.bat ingest
```

```bat
.\run-agent.bat ask "植物吸收碳 dioxide 受哪些条件影响？"
```

```bat
.\run-agent.bat ask "加强针如何改善免疫记忆？"
```

```bat
.\run-agent.bat ask "P waves 能穿过哪些介质？S waves 呢？"
```

```bat
.\run-agent.bat ask "乙醇沸点为什么比水低？"
```

```bat
.\run-agent.bat ask "量子纠缠如何用于加密？"
```

重复文件检测验证：

```bat
copy knowledge_base\raw\markdown\photosynthesis_carbon_cycle.md knowledge_base\raw\markdown\photosynthesis_carbon_cycle_copy.md
.\run-agent.bat ingest
.\run-agent.bat ask "影响光合作用速率的因素有哪些？"
del knowledge_base\raw\markdown\photosynthesis_carbon_cycle_copy.md
.\run-agent.bat ingest
```

预期结果：

- `ingest` 输出应包含 `indexed_at`、`embedding_model`、`duplicates`、`duplicate_files`。
- 语义相近但不完全同词的问题应能命中正确本地文件。
- 本地命中结果应包含 `citations` 数组。
- `sources` 中每条来源应包含 `citation`、`rerank_score`、`semantic_score`、`keyword_score`、`fts_score`。
- 查询结果应包含 `index_updated_at` 和 `embedding_model`。
- 复制重复文件后重新导入，`duplicates` 应大于 0，`duplicate_files` 应显示重复文件和原始文件路径。
- 重复文件不应导致查询结果里出现重复来源。
- 超出本地知识库范围的问题仍应返回 `source_type: "none"`、`need_web_search: true`。

## 第四阶段：做成更方便的使用方式

计划能力：

- `.\run-agent.bat ingest`
- `.\run-agent.bat ask "问题"`
- `.\run-agent.bat ask "问题" --web`
- `.\run-agent.bat sources`
- `.\run-agent.bat rebuild`

验证命令将在第四阶段完成后补充。
