# 示例目录

这里是本次创建的 agent 和 skill 示例入口，所有说明文档均使用中文。

## Agent 示例：客服工单分诊

目录：

- `examples/customer-support-agent/`

主要文件：

- `examples/customer-support-agent/README.md`：中文说明文档。
- `examples/customer-support-agent/agent.py`：可运行的 agent 示例。
- `examples/customer-support-agent/sample_ticket.json`：样例工单输入。

运行命令：

```powershell
python .\examples\customer-support-agent\agent.py
python .\examples\customer-support-agent\agent.py --ticket .\examples\customer-support-agent\sample_ticket.json
```

查看当前可见的 skills：

```powershell
python .\examples\customer-support-agent\agent.py --list-skills
```

直接输入工单内容：

```powershell
python .\examples\customer-support-agent\agent.py --subject "紧急退款" --body "我今天被扣费了，需要退款，否则会申请拒付。"
```

默认只输出工具结果和下一步动作；需要完整过程时添加 `--verbose`。

更方便的运行方式：

```powershell
.\run-agent.bat
```

不带参数运行时，会提示你输入工单标题和正文。

也可以直接传参：

```powershell
.\run-agent.bat "紧急退款" "我今天被扣费了，需要退款，否则会申请拒付。"
.\run-agent.bat ticket .\examples\customer-support-agent\sample_ticket.json
.\run-agent.bat skills
```

知识库导入和查询：

```powershell
.\run-agent.bat ingest
.\run-agent.bat ask "光合作用受哪些因素影响？"
.\run-agent.bat ask "加强针有什么作用？"
.\run-agent.bat ask "P waves 和 S waves 有什么区别？"
```

如果知识库没有找到依据，会返回 `need_web_search: true`，提示是否允许联网搜索。

如果你想使用 PowerShell 参数风格，也可以用：

```powershell
PowerShell -ExecutionPolicy Bypass -File .\run-agent.ps1 -Subject "紧急退款" -Body "我今天被扣费了，需要退款，否则会申请拒付。"
```

## Skill 示例：Markdown 报告润色

目录：

- `skills/markdown-report-polisher/`

主要文件：

- `skills/markdown-report-polisher/SKILL.md`：中文 skill 指令。
- `skills/markdown-report-polisher/scripts/inspect_markdown_report.py`：报告结构检查脚本。
- `skills/markdown-report-polisher/references/report-style.md`：中文报告风格参考。
- `examples/sample_status_report.md`：中文样例报告。

运行命令：

```powershell
python .\skills\markdown-report-polisher\scripts\inspect_markdown_report.py .\examples\sample_status_report.md
```

## 两者区别

Agent 更像一个能执行任务的程序：它接收输入、判断状态、调用工具、输出结果。

Skill 更像给 Codex 使用的能力包：它包含触发说明、工作流程、参考资料和可复用脚本，帮助 Codex 在某类任务上稳定执行。
