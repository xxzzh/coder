# 项目当前状态与下一步建议

生成时间：2026-07-18  
项目路径：`D:\codex\local-knowledge-agent`

## 1. 本次检查结论

当前项目已经达到“本地可运行、可演示、可打包交付”的阶段。核心审计、健康检查、检索护栏、答案精炼和 API provider 测试均通过；知识库已有索引，Web UI、命令行问答、OCR、Excel/Word/PDF/Markdown/TXT 导入、联网兜底、API 精炼、打包脚本和项目内审计脚本都已经具备。

但当前还不适合直接继续堆新功能。最应该先做的是整理交付基线：确认未提交改动的范围、处理 dist 与演示资产的版本策略、清理疑似敏感或低价值资料，再做一次严格重建和端到端 Web UI 验证。

## 2. 已执行检查

已执行并通过：

- `.\audit-local.bat`
- `python scripts\test_retrieval_guardrails.py`，由审计脚本调用，通过
- `python scripts\test_answer_refinement.py`，输出 `answer refinement: ok`
- `python scripts\test_api_providers.py`，输出 `api providers: ok`
- `python scripts\package_dist.py`，成功生成 `dist\local-knowledge` 和 `dist\local-knowledge.zip`

未完成的验证：

- `python scripts\run_excel_random_question_suite.py --help`
- `python scripts\run_webui_question_suite.py --help`

这两个命令在本次检查中超时，原因不是测试失败，而是脚本看起来不是普通 `--help` 入口，且依赖 Web UI / Playwright 运行环境。后续需要按端到端测试方式单独执行。

## 3. 当前项目状态

### 3.1 功能状态

项目定位清晰：本地知识库 RAG Agent，面向普通用户本机试用，不是多租户生产平台。

当前能力包括：

- 一键菜单：`start.bat`
- 统一命令入口：`run-agent.bat`
- Web UI：`scripts\web_agent.py`
- 知识库管理：`scripts\manage_knowledge_base.py`
- 导入与索引：`scripts\ingest_knowledge_base.py`
- 问答与检索：`scripts\query_knowledge_base.py`
- API 配置：`scripts\configure_api.py`
- 健康检查：`scripts\healthcheck_agent.py`
- 项目本地审计：`audit-local.bat` + `scripts\audit_local_project.py`
- 打包：`package-dist.bat` + `scripts\package_dist.py`
- 领域检索插件目录：`scripts\retrievers\`

### 3.2 知识库状态

审计结果显示：

- raw 支持文件数：12
- 索引文档数：12
- chunk 数：1769
- 重复文档数：0
- 索引更新时间：2026-07-08T14:41:44+00:00
- 分块策略：`semantic-boundary-local-embedding`
- embedding：`local-hash-ngram-384d`
- 当前建议：SQLite scan 仍可接受，暂不需要独立向量数据库

抽取质量：

- `requires_review`: 0
- `warnings`: 3
- `approved_reviews`: 3
- OCR 环境可用：Tesseract、pdftoppm、项目内 tessdata 均已识别

检索质量风险：

- `low_information_chunks`: 867 / 1769，比例偏高
- `fts_hit_rate`: 0.417

这说明项目可用，但检索质量还有优化空间，尤其是 Excel 大表、短文本和低信息 chunk 的处理。

### 3.3 API 与翻译状态

当前 `.env` 已配置 API，审计显示：

- `api_configured`: true
- `api_backend_usable`: true
- provider: `deepseek`
- 在线中文回答能力可用

离线英文到中文翻译：

- `argostranslate` 未安装
- 离线 en -> zh 未就绪

如果项目需要在无网或无 API 环境下保持英文资料中文回答能力，需要补装离线翻译模型；如果允许使用 DeepSeek，则当前可先不处理。

### 3.4 Git 工作区状态

当前工作区有大量未提交变更。

已修改的主要范围：

- `.gitignore`
- `AGENTS.md`
- `README.md`
- `scripts\*.py`
- `scripts\retrievers\*`
- `dist\local-knowledge\*`
- `dist\local-knowledge.zip`

未跟踪的主要范围：

- `audit-local.bat`
- `package-dist.bat`
- `scripts\audit_local_project.py`
- `scripts\package_dist.py`
- `scripts\retrievers\`
- `submission_assets\`
- `tmp\`
- 多份中文说明、演讲稿、HTML 展示文件
- `wenti.txt`
- `youhua.txt`

注意：`.gitignore` 已加入 `dist/`、`output/`、`require.txt`，但 `dist` 下已有文件仍处于被 Git 跟踪的修改状态。需要决定 dist 是否继续纳入版本库。

## 4. 主要风险

### 风险 1：交付边界不清

代码、dist 打包产物、演示视频、临时输出和说明材料混在一个工作区里。继续开发前应先决定哪些属于源码，哪些属于交付附件，哪些应该忽略。

### 风险 2：可能存在敏感资料

知识库 raw 中有 `mima .txt`，文件名看起来像“密码”。虽然审计只看到它是一个 24 字符文本文件，但它已经被索引。下一步应人工确认是否允许进入知识库和交付包。

### 风险 3：低信息 chunk 偏多

867 个低信息 chunk 会影响召回质量、排序质量和答案稳定性。当前检索护栏通过，说明已有规则兜底，但后续质量提升应优先处理这个问题。

### 风险 4：端到端 Web UI 验证未完成

Web UI 代码存在，审计通过，但本次没有完成真实浏览器端到端测试。交付给别人前应跑一次 Web UI 自动化问题集或至少人工完成关键路径验证。

### 风险 5：旧版 Office / OCR 依赖有环境差异

当前机器上 Word、Tesseract、pdftoppm 可用。交付到其他电脑时，`.doc`、扫描 PDF、图片表格的效果依赖这些工具是否安装或是否随项目一起可用。

## 5. 建议下一步操作

### 第一步：先清理交付基线

目标：不要继续新增功能，先把当前成果整理成一个可提交、可回滚、可交付的版本。

建议操作：

```bat
git status --short
git diff --stat
```

然后人工决定：

- `scripts\`、`README.md`、`AGENTS.md`、审计脚本、打包脚本：建议纳入提交
- `submission_assets\`：如果用于答辩/提交，建议单独提交或单独打包
- `tmp\`：建议不提交
- `dist\`：二选一，要么继续作为发布产物跟踪，要么从 Git 索引移除后只保留本地生成
- `mima .txt`：必须先确认内容是否可进入知识库

如果决定不再跟踪 dist，建议执行：

```bat
git rm -r --cached dist
```

如果决定保留 dist 作为交付产物，则应移除 `.gitignore` 中的 `dist/` 或接受“已跟踪文件仍可改动、新文件被忽略”的混合状态。

### 第二步：处理 raw 资料和敏感文件

先确认：

```bat
dir knowledge_base\raw
```

重点检查：

```text
knowledge_base\raw\mima .txt
```

如果它是密码、密钥、账号信息或私人信息，应从 raw 中移除并重建索引。

重建命令：

```bat
.\run-agent.bat rebuild-strict
.\audit-local.bat
```

### 第三步：补齐端到端 Web UI 验证

先启动 Web UI：

```bat
.\run-agent.bat web --open
```

再在另一个终端运行：

```bat
python scripts\run_webui_question_suite.py
python scripts\run_excel_random_question_suite.py
```

如果测试依赖 Playwright CLI 未就绪，先解决浏览器自动化环境；如果测试耗时较长，建议把结果输出到 `output\`，不要提交 `tmp\`。

### 第四步：优化检索质量

优先处理低信息 chunk：

- 对 Excel 表格分块增加“表头 + 行上下文 + sheet 名”的结构化描述
- 对短文本和空白行生成的 chunk 做过滤
- 对 `mima .txt` 这类极短或敏感文本设置排除规则
- 针对 N972 测试计划、Lenovo Dictionary、MES 主数据这三类高价值 Excel 加领域 retriever 测试

每轮优化后执行：

```bat
.\run-agent.bat rebuild-strict
.\audit-local.bat
python scripts\test_retrieval_guardrails.py
```

### 第五步：重新打包并做交付验收

完成清理和验证后执行：

```bat
python scripts\package_dist.py
.\audit-local.bat
```

然后检查：

- `dist\local-knowledge.zip` 是否存在
- 解压后能否在项目目录内运行 `.\audit-local.bat`
- 解压包内不依赖外部项目目录
- 包内不包含 `.env`、临时目录、私密资料或不该公开的视频素材

## 6. 推荐的立即行动

最建议马上做这一组动作：

```bat
git status --short
dir knowledge_base\raw
.\run-agent.bat rebuild-strict
.\audit-local.bat
.\run-agent.bat web --open
python scripts\run_webui_question_suite.py
python scripts\package_dist.py
```

如果这些都通过，再进行一次有意识的 Git 提交，把当前项目固定为“可交付版本”。提交前不要把 `.env`、`tmp\`、未经确认的敏感资料和无关输出提交进去。

## 7. 当前优先级判断

P0：确认并处理 `mima .txt`，整理 Git 交付边界。  
P1：跑通 Web UI 端到端验证。  
P2：优化低信息 chunk 和 Excel 检索质量。  
P3：完善答辩/演示材料与 dist 发布策略。  
P4：再考虑接入独立向量数据库或更复杂的多用户能力。

## 8. 2026-07-18 执行记录

已按建议完成以下操作：

- 已运行 `.\audit-local.bat`，初始审计通过。
- 已将 `knowledge_base\raw\mima .txt` 隔离到 `output\quarantine\raw-sensitive\mima .txt`，未删除原文件内容，但已移出 raw 索引范围。
- 已运行 `.\run-agent.bat rebuild-strict`，严格重建后索引变为 11 个文档、1768 个 chunk。
- 已再次运行 `.\audit-local.bat`，重建后审计通过。
- 已启动 Web UI，确认 `http://127.0.0.1:8765/` 返回 200。
- 已修复 Web UI `/favicon.ico` 404，当前返回 204。
- 已修正 WebUI 固定问题集断言逻辑，避免字段分隔符导致误报。
- 已修正 Excel 随机问题集规格符号归一化，避免 `*`、`~` 等格式符号导致误报。
- 已运行 `python scripts\run_webui_question_suite.py`，结果 23/23 通过。
- 已运行 `python scripts\run_excel_random_question_suite.py`，结果 40/40 通过。
- 已运行 `python scripts\test_answer_refinement.py`，通过。
- 已运行 `python scripts\test_api_providers.py`，通过。
- 已运行 `python scripts\package_dist.py`，重新生成 `dist\local-knowledge` 和 `dist\local-knowledge.zip`。
- 已检查 zip 内容，未发现 `.env`、`mima` 或 quarantine 路径。
- 已运行最终 `.\audit-local.bat`，结果通过。

当前仍需人工决策：

- 是否提交 `dist\`。当前 `.gitignore` 已忽略 `dist/`，但历史上 `dist` 文件已被 Git 跟踪，所以仍显示修改。
- 是否提交 `submission_assets\` 中的演示素材。
- 是否清理或忽略 `tmp\`、`.playwright-cli\`、`output\` 中的本地运行产物。
- 是否进一步优化 866 个 low-information chunks。
