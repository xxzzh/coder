# Local Knowledge Base Agent 项目框架说明

这份文档面向不懂技术的使用者和后续维护人员，用尽量直白的方式说明：这个项目里每个目录和文件是做什么的、平时应该改哪里、不要随便改哪里，以及常见修改应该怎么做。

## 一句话说明

这个项目是一个“本地知识库问答工具”。

使用者把 Word、PDF、Excel、Markdown、TXT 等资料放进 `knowledge_base/raw`，系统会把资料读取出来、切成便于搜索的小片段、建立本地索引。之后用户可以在浏览器页面或命令行里提问，系统会先从本地资料里找证据，再整理成答案，并附上资料来源。

## 给普通使用者的最短流程

1. 双击 `start.bat`。
2. 选择 `1` 完成首次配置。
3. 选择 `3` 打开资料目录。
4. 把资料放进 `knowledge_base/raw` 或它下面的分类文件夹。
5. 选择 `4` 严格重建索引。
6. 选择 `6` 健康检查，看到 `"ok": true` 说明基本可用。
7. 选择 `2` 打开网页界面，然后提问。

资料更新后，通常只需要再次双击 `start.bat`，选择 `5` 增量更新索引。

## 项目树结构总览

```text
local-knowledge-agent/
├─ start.bat                         # 给普通用户用的一键菜单，双击它就能操作
├─ run-agent.bat                     # 给熟悉命令的人用的命令入口，负责转发到具体 Python 脚本
├─ README.md                         # 项目原始说明书，偏技术和完整说明
├─ requirements.txt                  # 可选增强功能依赖清单，例如 PDF、OCR、Excel 读取能力
├─ .env                              # 本机配置文件，保存是否启用 API、API 地址、模型等
├─ .gitignore                        # 告诉 Git 哪些临时文件、索引文件、资料文件不要提交
├─ AGENTS.md                         # 给 Codex/开发助手看的项目工作规则
├─ install_kb_phase1.py              # 早期安装或初始化脚本，日常用户通常不用直接操作
├─ 分阶段开发与推送流程.md             # 开发流程记录，给维护人员参考
├─ 阶段验证命令.md                    # 开发阶段的验证命令记录，给维护人员参考
│
├─ knowledge_base/                   # 知识库资料和系统生成结果所在位置
│  ├─ raw/                           # 原始资料目录，普通用户主要改这里
│  │  ├─ markdown/                   # Markdown 资料，可放 .md 文件
│  │  ├─ word/                       # Word 资料，可放 .doc、.docx 文件
│  │  ├─ pdf/                        # PDF 资料，可放 .pdf 文件
│  │  └─ excel/                      # Excel 资料，可放 .xlsx 文件
│  ├─ processed/                     # 系统处理后的中间结果，不建议手动修改
│  │  ├─ documents.jsonl             # 记录已导入的文档信息
│  │  ├─ chunks.jsonl                # 记录被切分后的小段内容
│  │  ├─ faq.jsonl                   # 索引不可用时的兜底问答内容
│  │  ├─ extraction_report.jsonl     # 资料抽取质量报告，提示哪些文件可能漏读
│  │  └─ review_approvals.json       # 人工确认过的抽取风险记录
│  └─ index/                         # 本地搜索索引数据库，不建议手动修改
│     └─ knowledge.db                # SQLite 数据库，系统靠它快速搜索资料
│
├─ scripts/                          # 项目的主要功能脚本，维护人员主要看这里
│  ├─ setup_agent.py                 # 首次初始化，创建目录和 .env 配置
│  ├─ web_agent.py                   # 本地网页界面，浏览器里看到的页面和按钮来自这里
│  ├─ ingest_knowledge_base.py       # 导入资料、抽取文字、切分内容、建立索引
│  ├─ manage_knowledge_base.py       # 管理知识库，负责 update、rebuild、sources 等命令
│  ├─ query_knowledge_base.py        # 提问和检索，负责找资料、排序、生成回答
│  ├─ healthcheck_agent.py           # 健康检查，判断环境、资料、索引、OCR、API 是否正常
│  ├─ configure_api.py               # 命令行配置 API，用于答案精炼
│  ├─ api_providers.py               # API 提供商列表和调用逻辑，例如 OpenAI、DeepSeek、小米 MiMo
│  ├─ install_translation_model.py   # 安装离线英文到中文翻译模型
│  ├─ add_sample_knowledge_corpus.py # 添加示例知识资料，通常用于演示或测试
│  ├─ evaluate_ocr_test_materials.py # OCR 测试评估，检查扫描件识别效果
│  ├─ generate_ocr_test_materials.py # 生成 OCR 测试材料
│  ├─ run_excel_random_question_suite.py # Excel 问答测试脚本
│  ├─ run_webui_question_suite.py    # Web UI 问答测试脚本
│  ├─ test_answer_refinement.py      # 答案精炼相关测试
│  ├─ test_api_providers.py          # API 提供商相关测试
│  └─ test_retrieval_guardrails.py   # 检索保护规则测试，避免乱答
│
├─ test_materials/                   # 测试材料，主要给开发和验收使用
│  └─ ocr/                           # OCR 识别测试用 PDF、DOCX 和标准答案
│
├─ tools/                            # 项目自带工具资源
│  └─ tessdata/                      # OCR 语言包，例如中文、英文识别数据
│
├─ logs/                             # 日志目录，记录运行过程中的信息，通常可清理
├─ dist/                             # 打包或发布结果目录，普通用户通常不用改
├─ .codex/                           # Codex 项目技能和辅助配置，普通用户不用改
├─ .playwright-cli/                  # 浏览器自动化测试工具目录，普通用户不用改
├─ .git/                             # Git 版本管理目录，不能手动改
└─ __pycache__/                      # Python 自动生成的缓存目录，可忽略
```

## 模块作用说明

### 1. 用户入口模块

相关文件：

- `start.bat`
- `run-agent.bat`

作用：

`start.bat` 是最适合普通用户使用的入口。双击后会出现菜单，可以初始化、打开网页、打开资料目录、更新索引、健康检查、提问、查看来源。

`run-agent.bat` 更像是“总开关”。它会根据输入的命令，把任务交给 `scripts` 目录里的具体脚本处理。

常见修改例子：想修改菜单文字。

修改方法：

1. 用记事本或 VS Code 打开 `start.bat`。
2. 找到类似下面的菜单文字：

```bat
echo  2. Start Web UI
echo  3. Open raw materials folder
```

3. 改成更容易理解的中文，例如：

```bat
echo  2. 打开知识库问答网页
echo  3. 打开资料存放文件夹
```

4. 保存后重新双击 `start.bat` 查看效果。

注意：

只改 `echo` 后面的显示文字通常比较安全。不要随便改 `goto`、`call`、`if "%CHOICE%"` 这些命令逻辑。

### 2. 原始资料模块

相关目录：

- `knowledge_base/raw`
- `knowledge_base/raw/markdown`
- `knowledge_base/raw/word`
- `knowledge_base/raw/pdf`
- `knowledge_base/raw/excel`

作用：

这里是普通用户最常接触的地方。所有希望被问答系统学习和检索的资料，都应该放在这里。

支持的资料类型：

- `.md`
- `.txt`
- `.doc`
- `.docx`
- `.pdf`
- `.xlsx`

常见修改例子：新增一份产品说明书。

修改方法：

1. 双击 `start.bat`。
2. 选择 `3` 打开资料目录。
3. 如果是 Word 文件，把它放进 `word` 文件夹；如果是 PDF，放进 `pdf` 文件夹；如果不确定，也可以直接放进 `raw` 目录。
4. 回到菜单，选择 `5` 增量更新索引。
5. 如果希望彻底重新整理全部资料，选择 `4` 严格重建索引。
6. 选择 `6` 健康检查，确认 `"ok": true`。

注意：

不要把临时文件、压缩包、图片、视频直接当作知识资料放进去。当前系统主要支持文档和表格。如果放入不支持的文件，健康检查会提示。

### 3. 资料处理和索引模块

相关文件和目录：

- `scripts/ingest_knowledge_base.py`
- `scripts/manage_knowledge_base.py`
- `knowledge_base/processed`
- `knowledge_base/index`

作用：

这个模块负责把原始资料变成系统能搜索的形式。

可以把它理解成“资料整理员”：

1. 找到 `raw` 目录里的资料。
2. 读取 Word、PDF、Excel、Markdown、TXT 里的文字。
3. 如果是扫描件或图片内容，尝试用 OCR 识别文字。
4. 把长文档切成一小段一小段。
5. 建立本地搜索索引。
6. 生成抽取质量报告，提示哪些文件可能没有完整读出来。

重要文件说明：

- `knowledge_base/processed/documents.jsonl`：记录每份资料的信息。
- `knowledge_base/processed/chunks.jsonl`：记录每份资料被拆分后的片段。
- `knowledge_base/processed/extraction_report.jsonl`：记录抽取风险，例如某个 PDF 可能主要是图片。
- `knowledge_base/index/knowledge.db`：真正用于快速搜索的本地数据库。

常见修改例子：资料更新后重新建库。

推荐做法：

1. 资料只是新增或修改少量文件：在 `start.bat` 菜单里选择 `5`。
2. 资料改动很大，或者答案明显不对：在 `start.bat` 菜单里选择 `4`。
3. 重建后选择 `6` 健康检查。

注意：

`processed` 和 `index` 是系统生成结果，不建议手动改。改错后可能导致搜索结果混乱。需要重新生成时，用菜单里的“增量更新”或“严格重建”。

### 4. 问答检索模块

相关文件：

- `scripts/query_knowledge_base.py`

作用：

这个模块负责处理用户问题。

可以把它理解成“资料查询员 + 答案整理员”：

1. 接收用户提出的问题。
2. 在本地知识库里搜索相关资料片段。
3. 对搜索结果排序，优先选择更相关的内容。
4. 如果本地资料不够，并且允许联网，就尝试从公开网页搜索补充信息。
5. 如果配置了 API，就把找到的证据交给大模型整理成更自然的中文答案。
6. 输出答案和引用来源。

常见修改例子：只允许使用本地资料，不想联网。

使用方法：

```bat
.\run-agent.bat ask "这里输入你的问题" --no-web
```

如果是在日常菜单里使用，建议提醒用户：敏感资料或内部制度类问题，优先使用“不联网”的方式。

维护人员可修改的位置：

- 想调整“本地资料不够时是否联网”，看 `run-agent.bat` 里的 `--web` 参数。
- 想调整搜索、排序、缓存等规则，看 `scripts/query_knowledge_base.py`。

注意：

`query_knowledge_base.py` 是核心逻辑文件，不建议非技术人员直接修改。

### 5. 本地网页界面模块

相关文件：

- `scripts/web_agent.py`

作用：

这个文件负责浏览器里看到的本地网页界面。

网页功能包括：

- 输入问题并查看答案。
- 查看引用来源。
- 查看健康状态。
- 查看 OCR 状态。
- 增量更新索引。
- 严格重建索引。
- 查看来源列表。
- 配置 API。

常见修改例子：修改网页标题。

修改方法：

1. 打开 `scripts/web_agent.py`。
2. 搜索：

```html
<title>Local Knowledge Base Agent</title>
```

3. 改成：

```html
<title>公司知识库问答助手</title>
```

4. 保存文件。
5. 重新启动 Web UI。

注意：

网页的按钮、样式、接口逻辑大多都在这个文件里。只改文字风险较低；如果要改按钮功能，需要开发人员处理。

### 6. 初始化和健康检查模块

相关文件：

- `scripts/setup_agent.py`
- `scripts/healthcheck_agent.py`

作用：

`setup_agent.py` 负责首次配置。它会创建必要目录，检查 SQLite 搜索能力，并询问是否配置大模型 API。

`healthcheck_agent.py` 负责检查系统是否能正常工作。它会检查：

- Python 是否可用。
- 本地数据库是否存在。
- 索引里有没有资料。
- 是否存在不支持的文件。
- OCR 能力是否可用。
- API 配置是否合理。
- 是否有资料抽取后需要人工复核。

常见修改例子：检查项目是否准备好。

操作方法：

1. 双击 `start.bat`。
2. 选择 `6` 健康检查。
3. 找到输出里的：

```json
"ok": true
```

如果是 `true`，说明基本可用。如果是 `false`，需要看它同时提示了什么，例如没有资料、索引不存在、OCR 未安装、存在不支持的文件等。

### 7. API 和答案精炼模块

相关文件：

- `.env`
- `scripts/configure_api.py`
- `scripts/api_providers.py`

作用：

这个模块负责配置和调用大模型 API。API 不是必须的；没有 API 时，系统仍然可以做本地检索和引用输出。

配置 API 后，大模型主要用于把已找到的资料证据整理成更自然的答案。它不是直接脱离资料自由回答。

`.env` 里常见配置：

```env
LKA_USE_API=true
LKA_API_PROVIDER=openai-compatible
LKA_API_BASE_URL=https://api.openai.com/v1
LKA_API_KEY=your_api_key_here
LKA_API_MODEL=gpt-5-mini
LKA_API_TIMEOUT_SECONDS=20
```

常见修改例子：临时关闭 API。

修改方法：

1. 打开 `.env`。
2. 找到：

```env
LKA_USE_API=true
```

3. 改成：

```env
LKA_USE_API=false
```

4. 保存后重新提问。

注意：

`.env` 可能包含 API key，相当于密码。不要发给无关人员，也不要提交到公开仓库。

### 8. OCR 和扫描件识别模块

相关目录和文件：

- `tools/tessdata`
- `scripts/evaluate_ocr_test_materials.py`
- `scripts/generate_ocr_test_materials.py`
- `test_materials/ocr`

作用：

OCR 用来识别扫描 PDF、图片表格、截图类内容中的文字。

`tools/tessdata` 里放的是 OCR 语言包，例如：

- `chi_sim.traineddata`：简体中文识别。
- `eng.traineddata`：英文识别。
- `osd.traineddata`：方向检测。

常见修改例子：检查 OCR 是否正常。

操作方法：

1. 双击 `start.bat`。
2. 选择 `9` 运行 OCR 测试材料评估。
3. 如果识别效果不好，先确认电脑上是否安装了 Tesseract OCR 和 `pdftoppm`。
4. 安装好后，选择 `4` 严格重建索引。

注意：

如果资料主要是扫描件，OCR 能力会明显影响问答质量。

### 9. 测试和验收模块

相关目录和文件：

- `test_materials`
- `scripts/test_answer_refinement.py`
- `scripts/test_api_providers.py`
- `scripts/test_retrieval_guardrails.py`
- `scripts/run_webui_question_suite.py`
- `scripts/run_excel_random_question_suite.py`

作用：

这些内容主要给开发人员或维护人员使用，用来验证系统改动后有没有破坏原有功能。

常见修改例子：改完代码后想简单验证。

可执行：

```bat
python scripts\test_api_providers.py
python scripts\test_retrieval_guardrails.py
python scripts\test_answer_refinement.py
```

如果只是普通用户添加资料，不需要运行这些测试。

### 10. Codex 和开发辅助模块

相关目录：

- `.codex`
- `.playwright-cli`
- `.git`
- `dist`
- `logs`
- `__pycache__`

作用：

这些目录大多不是普通用户需要关心的内容。

- `.codex`：Codex 项目技能和开发助手配置。
- `.playwright-cli`：浏览器自动化测试工具。
- `.git`：版本管理信息，不能手动改。
- `dist`：打包发布结果。
- `logs`：运行日志。
- `__pycache__`：Python 自动缓存。

常见修改例子：清理日志。

可以删除 `logs` 里的旧日志文件，但不要删除 `.git`。如果不确定，先不要动。

## 普通维护人员最常改哪里

| 想做的事 | 应该改哪里 | 推荐方式 |
|---|---|---|
| 添加或替换知识资料 | `knowledge_base/raw` | 放入文件后运行增量更新或严格重建 |
| 修改菜单显示文字 | `start.bat` | 只改 `echo` 后面的文字 |
| 修改浏览器页面标题或提示语 | `scripts/web_agent.py` | 搜索原文字后替换 |
| 开关 API | `.env` 或菜单里的 API 配置 | 优先用 `start.bat` 菜单配置 |
| 查看系统是否正常 | 不用改文件 | 运行健康检查 |
| 重新生成索引 | 不用改文件 | 菜单选择增量更新或严格重建 |
| 排查资料是否漏读 | `knowledge_base/processed/extraction_report.jsonl` | 只查看，不建议手动改 |

## 不建议手动修改的内容

以下内容由系统自动生成或属于项目内部机制，不建议普通用户直接修改：

- `knowledge_base/index/knowledge.db`
- `knowledge_base/processed/documents.jsonl`
- `knowledge_base/processed/chunks.jsonl`
- `knowledge_base/processed/review_approvals.json`
- `.git`
- `__pycache__`
- `.playwright-cli`

如果这些内容出现问题，优先使用 `start.bat` 里的“严格重建索引”，不要手动打开数据库改内容。

## 常见修改场景示例

### 示例 1：新增一批公司制度文档

目标：

让系统能回答新制度相关问题。

操作：

1. 双击 `start.bat`。
2. 选择 `3` 打开资料目录。
3. 把 Word 或 PDF 制度文档放进 `knowledge_base/raw/word` 或 `knowledge_base/raw/pdf`。
4. 回到菜单，选择 `5` 增量更新索引。
5. 选择 `6` 健康检查。
6. 选择 `2` 打开网页提问。

如果回答没有包含新制度内容，选择 `4` 严格重建索引后再试。

### 示例 2：把菜单改成中文

目标：

让非技术人员更容易理解菜单。

操作：

1. 打开 `start.bat`。
2. 找到：

```bat
echo  1. First-time setup / configure API
echo  2. Start Web UI
echo  3. Open raw materials folder
```

3. 改成：

```bat
echo  1. 首次设置 / 配置 API
echo  2. 打开知识库问答网页
echo  3. 打开资料存放文件夹
```

4. 保存后重新双击 `start.bat`。

不要修改菜单下面的 `if "%CHOICE%"=="1" goto setup` 这类跳转逻辑。

### 示例 3：关闭联网兜底

目标：

只允许系统根据本地资料回答，避免使用外部网页。

临时使用：

```bat
.\run-agent.bat ask "你的问题" --no-web
```

如果要让默认提问也不联网，需要维护人员修改 `run-agent.bat`：

1. 打开 `run-agent.bat`。
2. 找到最后的直接提问逻辑：

```bat
:direct
python "%QUERY%" "%~1" --web
```

3. 改成：

```bat
:direct
python "%QUERY%" "%~1"
```

4. 保存。

这样直接输入问题时就不会默认加 `--web`。

### 示例 4：修改网页名称

目标：

让浏览器标签页显示公司自己的名称。

操作：

1. 打开 `scripts/web_agent.py`。
2. 搜索：

```html
<title>Local Knowledge Base Agent</title>
```

3. 改成：

```html
<title>公司知识库问答助手</title>
```

4. 保存并重新启动 Web UI。

### 示例 5：更换 API 模型

目标：

把答案精炼用的模型换成其他模型。

推荐操作：

1. 双击 `start.bat`。
2. 选择 `12` 配置答案精炼 API。
3. 选择 API 提供商。
4. 输入 API key。
5. 选择模型。
6. 等系统验证通过并保存。

不推荐普通用户手动改 `.env` 里的 API key 和模型名，容易写错。

### 示例 6：处理健康检查失败

目标：

让系统恢复到可用状态。

常见情况：

| 现象 | 可能原因 | 处理方式 |
|---|---|---|
| `"ok": false` | 还没有放资料 | 把资料放进 `knowledge_base/raw` 后重建索引 |
| 提示索引不存在 | 还没有建库 | 运行菜单 `4` 或 `5` |
| 提示有不支持文件 | `raw` 里放了不支持的文件 | 删除或移走不支持文件 |
| `requires_review` 大于 0 | 有文件可能没完整识别 | 查看抽取报告，安装 OCR 后重建 |
| OCR 不可用 | 没装 Tesseract 或 pdftoppm | 安装 OCR 工具后重建 |
| API 不可用 | key、地址或模型不正确 | 重新运行菜单 `12` |

## 这个项目的工作流程图

```text
用户放入资料
    ↓
knowledge_base/raw
    ↓
资料导入脚本读取文字
    ↓
切成小片段
    ↓
建立本地索引 knowledge.db
    ↓
用户在网页或命令行提问
    ↓
系统搜索本地资料
    ↓
找到相关片段并排序
    ↓
可选：调用 API 精炼答案
    ↓
输出答案和引用来源
```

## 后续维护建议

1. 普通资料更新，只动 `knowledge_base/raw`。
2. 出现回答不准，先严格重建索引，再健康检查。
3. 扫描件、图片表格、复杂 PDF 回答不准时，优先检查 OCR。
4. 不要把 `.env` 发给别人，因为里面可能有 API key。
5. 不要手动修改 `knowledge_base/index` 和 `knowledge_base/processed`，需要重建时用菜单操作。
6. 代码修改前，先复制一份项目或使用 Git 建分支，避免误改后无法恢复。

