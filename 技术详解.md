## 分块embedding得逻辑是什么？  --> 项目维度为384 

这个项目的“分块 + embedding”是纯本地逻辑，不调用外部 embedding 模型。

核心流程在 [ingest_knowledge_base.py (line 837)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:837)：

1. 文档先抽取成纯文本，然后 `normalize_text()` 统一换行和空白。
2. `chunk_text()` 按语义边界分块，不是固定窗口硬切。
3. 默认最大块长 `CHUNK_MAX_CHARS = 1100`，最小有效断点 `CHUNK_MIN_CHARS = 180`。
4. 先按空行拆成 block，Markdown 标题会尽量和后面的正文放一起。
5. 如果 block 太长：
    - 表格类文本按行拆。
    - 普通文本按句号、中文句末符号、分号、换行拆。
6. 合并 unit 时会判断是否该断块：
    - 合并后超过 1100 字符；
    - 新 unit 是标题；
    - 当前块和新 unit 的本地 embedding 余弦相似度低于 `0.08`。
7. 每个 chunk 生成一个 `chunk_id`，写入 `chunks` 表，同时写入 FTS5 表和 LSH 辅助表。

embedding 逻辑在 [ingest_knowledge_base.py (line 954)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:954)：

- 英文/数字 token：匹配 `[a-z0-9_]{2,}`。
- 中文：切 2、3、4 字 ngram。
- 每个 term 用 `blake2b` hash。
- hash 后映射到 384 维：`index = hash % 384`。
- 用 hash 位决定正负号。
- 权重是 `1.0 + min(len(term), 8) / 8.0`。
- 最后做 L2 normalize。
- 存储格式是稀疏向量 JSON，例如 `[[12,0.13],[91,-0.08]]`。

查询时在 [query_knowledge_base.py (line 759)](D:/codex/local-knowledge-agent/scripts/query_knowledge_base.py:759) 用同样逻辑给问题生成 embedding：

- 当前块数小于 `10000` 时，直接扫描所有 chunk embedding，算 cosine。
- 超过 `10000` 时，先用 `embedding_lsh` 做候选召回，再算 cosine。
- 最终不是只看 embedding，还会混合：
    - semantic score：0.48
    - keyword score：0.34
    - FTS score：0.18
    - 再乘以 chunk 信息质量分

所以它更准确地说是：**本地 hash-ngram sparse embedding + FTS5 + keyword rerank 的混合检索**。当前健康检查里也显示模型名是 `local-hash-ngram-384d`，分块策略是 `semantic-boundary-local-embedding`。

星期三22:30

## 你面对得是不懂这些技术得人员，请用简洁、通俗易懂得语言去解释，必要时可以举例说明

系统会先把一整份资料切成很多“小段”，再给每个小段做一个“内容指纹”。用户提问时，也会给问题做一个“问题指纹”，然后拿问题指纹去和所有资料小段的指纹比较，看哪些小段最像。

举例：

一份文档里有 3 部分：

```
1. HPD 测试项目说明
2. N972 工具使用步骤
3. API 配置方法
```

系统不会把整份文档一次性拿去匹配，而是会切成类似这样的小块：

```
块 1：HPD 测试项目说明……
块 2：N972 工具使用步骤……
块 3：API 配置方法……
```

然后每一块都会生成一个“内容指纹”。
当你问：

```
hpd test
```

系统会判断这个问题更像“块 1”，于是优先返回 HPD 测试相关资料。

这里的 embedding 不需要联网，也不是调用大模型。它更像是本地做的一套“关键词和语义相似度匹配”：

- 英文会看单词，比如 `hpd`、`test`
- 中文会切成短词，比如“测试项目”会拆成“测试”“项目”“测试项目”
- 然后把这些词变成数字指纹
- 查询时比较哪个资料小段和问题最接近

所以一句话总结：

**分块是把大资料切成容易查的小段；embedding 是给每个小段做本地指纹，方便系统快速找到和问题最相关的内容。**

星期三22:32

## 对于excel是怎么处理的

收到，后续默认用技术语言说明，只有你明确要求“通俗解释”时我再切换。

Excel 处理链路大致是：

1. **读取 `.xlsx`**
    代码在 [ingest_knowledge_base.py (line 450)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:450)。

    它不是用 Excel COM，也不是主要依赖 `openpyxl`，而是直接把 `.xlsx` 当 zip 解开，读取里面的 XML：

    - `xl/sharedStrings.xml`：读取共享字符串
    - `xl/workbook.xml`：读取工作表名称
    - `xl/worksheets/sheet*.xml`：读取每个 sheet 的行和单元格
    - 支持 `inlineStr`
    - 支持共享字符串 `t="s"`
    - 普通数字/文本读 `m:v`

2. **转成纯文本表格**
    每个 sheet 会变成：

    ```
    工作表 Sheet1
    单元格1 | 单元格2 | 单元格3
    单元格1 | 单元格2 | 单元格3
    ```

    也就是说，Excel 最终会被转换成“带工作表标题的管道分隔文本”。

3. **进入统一分块逻辑**
    转成文本后，Excel 和 `.md`、`.txt`、`.pdf` 一样进入 `chunk_text()`。

    但表格有特殊处理：如果一个 block 里包含 `|` 和换行，说明它像表格，会按行拆分，避免把大表格硬塞进一个超长 chunk。

    相关代码在 [ingest_knowledge_base.py (line 906)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:906)。

4. **每个 chunk 生成 embedding + FTS**
    每个 Excel chunk 会：

    - 写入 `chunks` 表
    - 生成本地 `local-hash-ngram-384d` embedding
    - 写入 `embedding_lsh`
    - 写入 `chunks_fts`

    相关写入逻辑在 [ingest_knowledge_base.py (line 1549)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:1549)。

5. **查询时有两套路径**
    普通 Excel 内容走通用检索：

    - FTS5 关键词匹配
    - 本地 embedding 相似度
    - keyword score
    - rerank 综合排序

    另外，项目里还有针对特定 Excel 的结构化检索，比如 `N972-ACER_Test plan_V3.1.xlsx`。
    这部分会按测试项、工具、门限、测试方案、判定方式等字段解析，而不是只靠语义检索。

    相关代码在 [query_knowledge_base.py (line 1591)](D:/codex/local-knowledge-agent/scripts/query_knowledge_base.py:1591)。

6. **限制**
    当前 Excel 处理主要读取单元格文本值：

    - 不计算公式，只读取文件里已有的值
    - 图表、图片、形状里的文字不作为可靠结构化内容
    - 合并单元格、复杂格式主要会被降级成文本行
    - 如果图表/绘图存在，会在抽取报告里提示可能需要人工复核

总结：
**Excel 先被解析成“工作表 + 行文本”，然后走统一分块、embedding、FTS 检索；对于特定业务表，例如 N972 测试计划，还额外有字段级结构化解析。**

## 将其他文件类型的处理方式也讲解一下

支持的原始资料类型在 [ingest_knowledge_base.py (line 27)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:27)：

```
.md, .txt, .doc, .docx, .pdf, .xlsx
```

除了 Excel，其他类型处理如下。

**通用流程**
所有文件都会走同一套后处理：

1. 抽取文本。
2. 生成抽取报告：抽取方式、字符数、warning、是否需要人工复核。
3. 文本标准化：统一换行、压缩空格、多余空行。
4. 分块。
5. 每个 chunk 写入：
    - `documents`
    - `chunks`
    - `chunks_fts`
    - `embedding_lsh`

入口在 [read_document_with_report (line 813)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:813)。

**`.md` / `.txt`**
处理最简单：

- 按文本文件直接读取。
- 编码尝试顺序：
    - `utf-8-sig`
    - `utf-8`
    - `gb18030`
    - 最后用 `utf-8 errors=ignore`

代码在 [read_plain_text (line 132)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:132)。

这类文件不会因为文本太短自动标记复核。

**`.docx`**
`.docx` 本质是 zip 包，代码直接解压读 XML：

- 读取 `word/document.xml`
- 遍历 Word 段落 `w:p`
- 提取文本节点 `w:t`
- 保留 tab、换行
- 拼成纯文本

代码在 [read_docx (line 241)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:241)。

另外还会检查 `word/media/` 里的嵌入图片：

- 如果图片 OCR 成功，把 OCR 文本追加到正文。
- 如果有图片但 OCR 失败，会写 warning，并标记 `requires_review = True`。

代码在 [read_docx_with_report (line 370)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:370)。

**`.doc`**
旧版 `.doc` 不直接解析。

流程是：

1. 调用 Windows + Microsoft Word COM。
2. 把 `.doc` 转成临时 `.docx`。
3. 再按 `.docx` 的逻辑抽取文本。
4. 记录抽取方式 `word_com_doc_to_docx`。

代码在 [convert_doc_to_docx (line 304)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:304) 和 [read_doc_with_report (line 357)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:357)。

限制：

- 必须在 Windows。
- 需要 PowerShell。
- 需要本机装了 Microsoft Word。
- 打开 Word 时是只读、隐藏窗口、禁用宏自动化安全风险。

**`.pdf`**
PDF 有多级 fallback，尽量从可靠到兜底：

1. 优先 

    ```
    pdfplumber
    ```

    - 提取页面文本。
    - 同时提取表格，表格转成 `|` 分隔行。

2. 再尝试 `pypdf` 或 `PyPDF2`。

3. 再尝试 Poppler 的 `pdftotext -layout`。

4. 如果都失败，再扫描 PDF stream 里的 `Tj/TJ` 文本片段。

5. 如果 PDF 疑似包含图片，或者抽出来的文本太短，会尝试 OCR。

相关代码：

- [read_pdf_with_report (line 753)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:753)
- [ocr_pdf (line 677)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:677)
- [read_pdf (line 712)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:712)

OCR 依赖：

- `pdftoppm`：把 PDF 页面渲染成图片。
- `tesseract`：识别图片文字。
- 语言默认 `eng+chi_sim`，如果本地缺语言包，会降级到可用语言。

如果 PDF 有图片但 OCR 不可用，会标记需要复核。

**复核逻辑**
非 `.md/.txt` 文件如果抽取文本少于 `80` 个字符，会认为可能是：

- 图片型文件
- 加密文件
- 版式复杂文件
- 抽取失败或不完整

然后写 warning，并设置 `requires_review = True`。

代码在 [read_document_with_report (line 829)](D:/codex/local-knowledge-agent/scripts/ingest_knowledge_base.py:829)。

总结：

- `.md/.txt`：直接读文本。
- `.docx`：解压 Word XML，附带图片 OCR。
- `.doc`：先用 Word 转 `.docx`，再处理。
- `.pdf`：多种文本抽取方式 + 表格抽取 + 必要时 OCR。
- `.xlsx`：解压 Excel XML，转成工作表/行文本，再额外支持部分结构化表格解析。

## embedding_lsh是什么意思

`embedding_lsh` 是一个**加速查找相似内容的辅助表**。

先说背景：

每个资料分块都会生成一个 embedding，也就是这个分块的“内容指纹”。
查询时，问题也会生成一个 embedding。系统要找：

```
哪些资料分块的 embedding 和问题 embedding 最像？
```

如果资料很少，可以直接把问题和所有分块逐个比较。

但如果分块很多，比如几万、几十万块，逐个比较会慢。
这时候就用 `embedding_lsh` 先做一轮粗筛。

`LSH` 全称是 `Locality-Sensitive Hashing`，可以理解成：

> 把相似的内容尽量分到相同或相近的桶里。

在这个项目里，`embedding_lsh` 表里存的是：

```
bucket -> chunk_id
```

也就是：

```
某个相似度桶里，有哪些资料分块
```

查询时流程是：

1. 给问题生成 embedding。
2. 根据问题 embedding 算出几个 bucket。
3. 去 `embedding_lsh` 表里找同 bucket 的 chunk。
4. 只对这些候选 chunk 计算真正的相似度。
5. 再排序返回最相关结果。

举例：

```
问题：hpd test
```

系统生成问题指纹后，发现它属于这些桶：

```
bucket A
bucket F
bucket K
```

于是先从 `embedding_lsh` 里找也落在这些桶里的资料块。
如果某些资料块也在这些桶里，说明它们“可能相关”，再进入精确比较。

所以一句话：

**`embedding_lsh` 不是最终答案，也不是新的 embedding 模型；它只是一个索引表，用来在资料很多时更快找到可能相似的分块。**

当前代码里，只有当 chunk 数量达到 `10000` 以上时，才优先使用这个 LSH 候选筛选；小知识库会直接扫描全部 embedding。

## hash n-gram embedding n如何解释

这里的 `n` 指每个字符片段包含多少个字符，不是向量维度。

例如“测试门限”：

- `n=2`：测试、试门、门限
- `n=3`：测试门、试门限
- `n=4`：测试门限

实际通常使用范围，例如 `n=2～4`，而不是单一值。

选择原则：

- `n` 小：召回率高，能容忍错字和短词，但噪声、哈希冲突更多。
- `n` 大：匹配更精确，但换一种表达就可能召回不到。
- 中文技术资料通常可先试 `2～4`。
- 编号、缩写较多时可保留 `2-gram`。
- 长英文单词或需要更高精度时可增加到 `3～5`。

最可靠的方法是准备一组真实问题，分别测试不同范围，例如 `2–3`、`2–4`、`3–5`，比较 `Recall@K`、MRR、误召回率和查询耗时，再确定参数。

目前项目的 CodeGraph 尚未初始化，所以我暂时不能准确确认代码里实际配置的 `n`。我注意到这个项目还没有初始化 CodeGraph，需要我运行 `codegraph init -i` 构建索引，然后帮你查明当前具体取值吗？

14:03

模型已从 GPT-5.6 Sol 更改为 GPT-5.5。

需要

14:07

已处理 11m 14s

需要的话我已经查准了：这个项目里 `hash n-gram embedding` 的 `n` 当前定义为 **中文 2、3、4 字符 gram**，不是单个 `n`，而是同时取 `(2, 3, 4)`。

位置在：

- [scripts/ingest_knowledge_base.py (line 33)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:33)：`EMBEDDING_DIMS = 384`
- [scripts/ingest_knowledge_base.py (line 962)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:962)：`embedding_terms()` 里中文按 `(2, 3, 4)` 切 n-gram
- [scripts/query_knowledge_base.py (line 56)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:56)：查询侧也是 `EMBEDDING_DIMS = 384`
- [scripts/query_knowledge_base.py (line 402)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:402)：查询时用同样的本地 hash embedding 算相似度

简单理解：

`n` 就是连续取多少个字作为一个特征。比如“测试门限”：

```
n=2: 测试、试门、门限
n=3: 测试门、试门限
n=4: 测试门限
```

本项目不是只选一个，而是把 `2/3/4` 都加入特征池，然后用 `blake2b` 哈希到 **384 维**向量里，所以模型名显示为 `local-hash-ngram-384d`。

怎么判断 `n` 的值合不合适：

中文技术文档里，`2-4` 是比较稳的选择。`n=2` 负责召回，能抓住短词、缩写和局部匹配；`n=3/4` 负责精度，能减少“只碰到一个字或泛词就误召回”的情况。

### 如果答辩时问，你可以说：

本项目采用多粒度 n-gram，兼顾短术语召回和长术语精确匹配；最终值不是拍脑袋，而是通过问答命中率、误召回率、Top-K 结果相关性和响应耗时来验证。

补一句：这套实现里 `n` 目前是代码硬编码的 `(2, 3, 4)`，不是配置文件动态读取。



## 项目里的向量数据库体现在哪里

项目里的“向量数据库”体现在本地 SQLite 索引库里，不是独立的 Milvus/Qdrant/Chroma 服务。
核心位置是这个数据库文件：
knowledge_base/index/knowledge.db
代码里定义在：
[scripts/ingest_knowledge_base.py (line 29)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:29)
[scripts/query_knowledge_base.py (line 41)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:41)
具体体现有三层：
chunks.embedding 存向量本体
在 [scripts/ingest_knowledge_base.py (line 1082)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:1082) 创建 chunks 表，其中 embedding TEXT NOT NULL 字段保存每个分块的本地 hash n-gram 向量。向量不是二进制，而是 JSON 文本形式，例如 [[维度索引, 权重], ...]。

embedding_lsh 存向量索引桶
在 [scripts/ingest_knowledge_base.py (line 1102)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:1102) 创建 embedding_lsh 表，字段包括 bucket、chunk_id、doc_id、indexed_at。它相当于向量召回的粗筛索引，用来在 chunk 很多时快速找到可能相似的分块。

查询时用 semantic_candidates() 做向量召回
在 [scripts/query_knowledge_base.py (line 759)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:759)，问题会先生成同样的本地 embedding，然后和库里 chunk 的 embedding 算 cosine 相似度。小规模时直接扫 chunks.embedding，大规模时先走 embedding_lsh 粗筛，再算相似度。

### 答辩时可以这样说：

本项目没有引入独立向量数据库，而是采用轻量化本地向量索引方案：用 SQLite 作为统一存储，chunks.embedding 保存分块向量，embedding_lsh 作为向量候选召回索引，再结合 FTS5 全文检索和关键词重排完成混合检索。对于当前本地知识库规模，这种方案部署简单、离线可用、维护成本低；当数据量达到几万 chunk 以上时，可以迁移到 FAISS、Qdrant、Milvus 或 sqlite-vec。

## 重排和召回及rerank怎么操作的呢？ 如果没有独立的向量数据库

rerank 和“重排”是一个意思。它和“召回”的区别是：
召回：先从大量 chunk 里捞出一批“可能相关”的候选。
重排：再给这些候选重新打分，选出最相关的 Top-K。
这个项目即使没有独立向量数据库，也能做召回和重排，因为它把“向量库能力”拆到了 SQLite + Python 逻辑里。
流程是这样的：
入库阶段先生成可检索数据
文档被切成 chunk 后，每个 chunk 会生成本地 hash n-gram embedding，然后写入 SQLite：
chunks.embedding：保存 chunk 的 384 维稀疏向量
chunks_fts：保存全文检索索引，走 SQLite FTS5
embedding_lsh：保存向量桶，用于大规模时粗筛候选
对应代码在 [scripts/ingest_knowledge_base.py (line 1560)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:1560)。
查询阶段做三路召回
在 [scripts/query_knowledge_base.py (line 1086)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1086) 的 search() 里，候选来源有三路：
rows = [
    *fts_candidates(...),
    *semantic_candidates(...),
    *fallback_like_candidates(...),
]
ranked = rerank(rows, ...)
也就是：
fts_candidates()：全文关键词召回，走 chunks_fts MATCH 和 bm25
semantic_candidates()：向量语义召回，问题也生成 embedding，然后和 chunk embedding 算 cosine
fallback_like_candidates()：兜底 LIKE 召回，避免 FTS 或向量没命中时完全没结果
没有独立向量库时，语义召回这样做
在 [scripts/query_knowledge_base.py (line 759)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:759)：
如果 chunk 数量少于 10000：直接从 SQLite 读出所有 chunks.embedding，在 Python 里算 cosine 相似度。
如果 chunk 数量大于等于 10000：先用 embedding_lsh 找同 bucket 的候选 chunk，再对候选算 cosine。
如果 LSH 找到的候选太少：回退到全量 embedding 扫描。
所以没有 Milvus/Qdrant 也可以做，只是这里用的是轻量本地方案：SQLite 存向量，Python 算相似度，LSH 表做粗筛。
重排公式
在 [scripts/query_knowledge_base.py (line 1044)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1044) 的 rerank() 里，系统会先按 chunk_id 去重合并候选，然后计算最终分：
rerank_score =
(0.48 * semantic_score
 + 0.34 * keyword_score
 + 0.18 * fts_score)
 * information_quality_score
    含义是：
    semantic_score：向量余弦相似度，占 48%
    keyword_score：关键词/句子匹配分，占 34%
    fts_score：FTS5/BM25 全文检索分，占 18%
    information_quality_score：内容质量系数，过滤低信息量 chunk

  ### 答辩可以这样讲：

  本项目没有依赖独立向量数据库，而是采用本地轻量混合检索。召回阶段同时使用 FTS5 全文召回、本地 hash n-gram embedding 语义召回和 LIKE 兜底召回；重排阶段将语义相似度、关键词匹配度、全文检索分和文本质量分融合，得到最终 rerank_score。这种方式适合本地知识库和中小规模资料，部署简单、离线可用；数据量更大时可以平滑替换为 Qdrant、Milvus、FAISS 等专业向量库。

## 这里的semantic_score：

向量余弦相似度，占 48%
keyword_score：关键词/句子匹配分，占 34%
fts_score：FTS5/BM25 全文检索分，占 18%
information_quality_score：内容质量系数，过滤低信息量 chunk 这4个分块的分数怎么计算的

这 4 个分数不是模型“黑盒打分”，都是代码里明确算出来的。核心在 [scripts/query_knowledge_base.py (line 1044)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1044) 的 rerank()。
最终公式是：
rerank_score =
(0.48 * semantic_score

 + 0.34 * keyword_score
 + 0.18 * fts_score)
 * information_quality_score
    semantic_score：向量余弦相似度
    位置：[scripts/query_knowledge_base.py (line 402)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:402)
    计算方式：
    问题生成本地 hash n-gram embedding。
    chunk 里已有入库时生成的 embedding。
    两个向量做点积：
    sum(value * right.get(index, 0.0) for index, value in left.items())
    因为向量在生成时已经归一化，所以这个点积就是 cosine 相似度。越接近 1，表示问题和 chunk 越相似；小于 0 的会被压成 0：
    semantic_score = max(0.0, semantic_score)
    keyword_score：关键词命中分
    位置：[scripts/query_knowledge_base.py (line 504)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:504)
    先从问题里抽候选词，包括中文连续词、英文词、别名扩展等。然后看这些词是否出现在 chunk 文本里：
    sum(3 if len(term) >= 4 else 1 for term in terms if term.lower() in lowered)
    也就是说：
    短词命中：加 1 分
    长词，长度大于等于 4：加 3 分
    最后会除以当前候选里的最大关键词分，归一化成 0-1
    keyword_score = term_score / max_term
    所以它表示“这个 chunk 对问题关键词的覆盖程度”。
    fts_score：FTS5/BM25 全文检索分
    位置：[scripts/query_knowledge_base.py (line 738)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:738)
    FTS 先用 SQLite FTS5 查：
    bm25(chunks_fts) AS fts_rank
    SQLite 的 BM25 分数通常是越小越相关，所以项目里转成越大越好的分数：
    fts_score = 1.0 / (1.0 + abs(fts_rank))
    如果这个 chunk 不是 FTS 召回来的，没有 fts_rank，那就是：
    fts_score = 0.0
    information_quality_score：内容质量系数
    位置：[scripts/query_knowledge_base.py (line 894)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:894)
    它不是相关性分，而是一个“质量惩罚/加权系数”。
    规则大概是：
    空文本：0.0
    Markdown 目录类内容：0.3
    枚举值类内容，且包含“枚举/值”等特征：1.25
    表头很多但细节少：0.35
    像短表头摘要：0.55
    普通有效内容：1.0
    所以它的作用是：避免目录、空壳表头、低信息 chunk 排到前面；同时对枚举表、参数值这类高价值结构化内容适当加权。

  ### 答辩时可以这样说：

  semantic_score 解决“意思像不像”，keyword_score 解决“关键词有没有命中”，fts_score 解决“全文检索相关性”，information_quality_score 解决“这段内容有没有足够信息量”。最后系统把四者融合成 rerank_score，所以不是单纯靠向量，也不是单纯靠关键词，而是混合重排。



## 帮我列举出完整的查询流程，要全面不要遗漏，流程中涉及到的技术也一一列举出来，从开始询问到输出答案

下面是这个项目从“用户提问”到“输出答案”的完整查询流程，按真实代码链路整理。核心入口是 [scripts/query_knowledge_base.py (line 3836)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:3836) 的 query()。
完整流程
用户输入问题
用户通过命令行、Web UI 或 Agent 调用传入：
question, limit, use_web, allow_api
涉及技术：
Python CLI / Web UI 调用
参数控制：limit 控制返回证据数量，use_web 控制是否允许联网，allow_api 控制是否允许调用大模型总结
读取运行配置
系统读取 .env 和环境变量，获取 API 配置：
LKA_USE_API
LKA_API_PROVIDER
LKA_API_BASE_URL
LKA_API_KEY
LKA_API_MODEL
LKA_API_TIMEOUT_SECONDS
涉及技术：
环境变量
OpenAI-compatible API 配置
本地/联网/大模型开关控制
判断本地知识库索引是否存在
默认数据库路径是：
knowledge_base/index/knowledge.db
如果数据库不存在，系统不会直接崩溃，而是进入降级流程：
先尝试 faq_fallback
如果允许联网，则走 Web 检索
如果没有可用证据，则返回“本地索引不存在，请先 ingest”的提示
涉及技术：
SQLite 文件检测
FAQ fallback
降级回答机制
用户引导信息
触发异步更新检查
如果数据库存在，系统会检查原始资料是否有变化，必要时触发异步更新。
涉及技术：
source fingerprint
异步索引更新标记
index_status: ready / updating
查询缓存检查
系统先查 query_cache 表，看相同问题是否已有可复用答案。
缓存判断会考虑：
问题文本
limit
是否联网
API 使用模式
当前索引版本 indexed_at
缓存 TTL
热点查询命中次数
涉及技术：
SQLite query_cache
JSON 序列化答案
TTL 缓存
热点查询缓存
索引版本一致性校验
优先走领域结构化检索器
在普通 RAG 检索前，系统会先尝试专用检索器：
abbreviation
station_test
station_test FFT
excel_table / N972
对应代码在 [scripts/query_knowledge_base.py (line 3813)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:3813)。
它会优先处理这类问题：
缩写/术语含义
某站位有哪些测试项
FFT 站位测试项
N972 Excel 表字段查询
特定结构化表格问题
涉及技术：
插件式 DomainRetriever
表格结构化解析
Excel 行列字段解析
规则匹配
专用业务检索
如果结构化检索命中，直接生成结构化答案
结构化答案不一定再走大模型，它可以直接输出基于表格/字段的确定性答案。
涉及技术：
规则模板生成答案
本地证据引用
answer_mode: local_structured_*
引用校验
如果结构化检索未命中，进入通用混合检索
主检索函数是 [scripts/query_knowledge_base.py (line 1086)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1086) 的 search()。
它会先做问题改写：
原始问题 → 归一化 → 别名扩展 → 检索问题
例如把业务别名、缩写、相关表达扩展进去。
涉及技术：
查询归一化
别名扩展
查询意图判断
迭代检索判断
三路召回候选 chunk
系统不是只靠一种检索，而是三路召回：
fts_candidates
semantic_candidates
fallback_like_candidates
第一路：FTS5 全文召回
使用 SQLite FTS5 的 MATCH 和 bm25()。
涉及技术：
SQLite FTS5
BM25
全文倒排索引
中文 n-gram 辅助 searchable text
第二路：语义向量召回
问题生成本地 hash n-gram embedding，和 chunks.embedding 里的 chunk 向量算余弦相似度。
涉及技术：
本地 hash n-gram embedding
384 维稀疏向量
中文 2/3/4 字 n-gram
blake2b hashing
cosine similarity
SQLite 存储向量 JSON
第三路：LIKE 兜底召回
如果 FTS 或语义召回没覆盖，系统还会用 SQL LIKE 做关键词兜底。
涉及技术：
SQL LIKE
关键词命中
小库全量扫描 / 大库 SQL 粗筛
大规模时使用 embedding_lsh 加速
如果 chunk 数量达到阈值，系统不会直接全量扫所有 embedding，而是先用 embedding_lsh 找候选桶。
流程是：
问题 embedding → 计算 LSH bucket → 查 embedding_lsh → 得到候选 chunk → 再算 cosine
涉及技术：
LSH 局部敏感哈希
向量桶索引
SQLite 辅助索引表
候选粗筛
精排 cosine
候选合并去重
三路召回可能命中同一个 chunk，系统会按 chunk_id 合并。
如果同一个 chunk 有多个分数来源，会保留更好的分数。
涉及技术：
chunk_id 去重
多路召回融合
score 合并
Rerank 重排
重排函数是 [scripts/query_knowledge_base.py (line 1044)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1044) 的 rerank()。
最终分数：
rerank_score =
(0.48 * semantic_score

 + 0.34 * keyword_score
 + 0.18 * fts_score)
 * information_quality_score
    涉及技术：
    语义相似度
    关键词匹配
    BM25 转换分
    信息质量加权
    Top-K 排序
    扩展上下文
    重排后，系统还会补充上下文：
    expand_fft_manual_table_context
    expand_section_context
    也就是如果命中了某个 chunk，可能会把同一节、相邻表格、相关上下文一起带上，避免答案断裂。
    涉及技术：
    chunk 上下文扩展
    表格上下文补全
    section 级上下文恢复
    相关性过滤
    系统会判断本地结果是否足够相关。
    如果不相关：
    不强答
    返回无本地依据
    如果允许联网，则转 Web 检索
    如果不允许联网，则给出本地无匹配和建议问题
    涉及技术：
    local relevance check
    阈值过滤
    防幻觉保护
    用户引导建议
    根据问题类型调整来源数量
    如果是“流程、步骤、机制、有哪些、列表、枚举”等问题，系统会进入迭代检索模式，保留更多来源。
    普通问题则保留更高分、更集中的来源。
    涉及技术：
    查询意图识别
    动态 Top-K
    分数阈值裁剪
    构造 sources 和 citations
    每个入选 chunk 会生成来源对象：
    source_id
    file
    file_path
    file_type
    chunk_id
    excerpt
    citation
    rerank_score
    semantic_score
    keyword_score
    fts_score
 *   涉及技术：
      证据片段抽取
    引文编号
    chunk 溯源
    文件级来源追踪
    先生成本地草稿答案
    系统会用 build_answer() 基于 sources 生成本地答案草稿。
    如果命中特定结构化场景，还会用 structured_local_answer() 替换成结构化答案。
    涉及技术：
    grounded answer draft
    citation-based answer
    结构化本地答案模板
    可选联网增强
    如果本地证据不足且 use_web=True，系统会调用联网研究流程。
    大致过程：
    搜索引擎检索 → 网页抓取 → 正文抽取 → 网页 chunk 打分 → 和本地结果合并
    涉及技术：
    百度 / Bing / DuckDuckGo / Wikipedia / Jina Bing 等搜索源
    HTTP 抓取
    HTML 正文抽取
    URL 安全过滤
    Web evidence rerank
    本地证据 + 网页证据融合
    最终答案生成 finalize_answer()
    最终统一进入 [scripts/query_knowledge_base.py (line 3692)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:3692) 的 finalize_answer()。
 *   它会做：
      附加查询结构信息
    判断是否允许 API
    如果是结构化答案，直接返回
    如果允许 API，用大模型基于 citations 总结
    如果不允许 API，用本地中文 fallback 总结
    校验引用
    删除未引用来源
    润色内部答案
 *   涉及技术：
      grounded API synthesis
    OpenAI-compatible chat completion
    prompt 约束：只能基于 citations 回答
    中文化回答
    citation validation
    fallback summary
    source pruning
    API 总结时的约束
    如果调用大模型，系统提示词要求：
    只能根据给定 citations 回答
    必须用简体中文
    不得编造
    证据不足要说明
    引用格式使用 [1]
    本地和网页证据要融合成直接答案
    涉及技术：
    RAG grounded generation
    citation-constrained prompting
    temperature 0.1
    API 超时控制
    API 失败回退
    引用校验和输出清理
    答案生成后会验证：
    答案里的 [1] [2] 是否真的存在
    未被引用的 sources 是否需要裁剪
    结构化答案是否保持来源
    API 失败时是否回退到本地答案
    涉及技术：
    citation validation
    hallucination guardrail
    source pruning
    answer polishing
    写入缓存
    最终答案会写回 query_cache，下次相同问题可以复用。
    涉及技术：
    SQLite cache
    JSON answer storage
    index version binding
    cache variant 区分本地/联网/API 模式
    返回最终 JSON
    最终输出不是纯文本，而是结构化结果，通常包含：
    answer
    source_type
    sources
    citations
    retrieval_mode
    embedding_model
    chunk_strategy
    semantic_candidate_strategy
    api_used
    web_search_used
    index_status
    cache_hit
    涉及技术：
    JSON 输出
    可解释检索元数据
    来源证据
    模型与索引策略透明化

  ### 一句话答辩版

  本项目的查询流程是：用户问题进入系统后，先读取运行配置和缓存，再判断本地索引状态；如果是缩写、站位、Excel 等业务问题，优先走结构化检索器；否则进入通用混合检索，通过 FTS5 全文召回、本地 hash n-gram embedding 语义召回、SQL LIKE 兜底召回三路获取候选 chunk，再用语义分、关键词分、BM25 分和内容质量分进行 rerank；随后扩展上下文、过滤低相关结果、生成 sources 和 citations，必要时融合联网证据，最后通过本地模板或 API grounded summary 生成带引用的中文答案，并写入缓存返回。

## 我记得不是低chunk用FTS、grep检索嘛？ 高chunk用混合检索，现在是全部都用混合检索嘛？

你记得的方向有一部分是对的，但更准确地说：现在代码里低 chunk 和高 chunk 都走混合检索，区别不是“是否混合”，而是语义向量召回怎么做。
当前主流程在 [scripts/query_knowledge_base.py (line 1086)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:1086)：
rows = [
    *fts_candidates(...),
    *semantic_candidates(...),
    *fallback_like_candidates(...),
]
ranked = rerank(...)
所以无论 chunk 数量多少，都会同时尝试三路召回：
fts_candidates：SQLite FTS5 / BM25 全文检索
semantic_candidates：本地 hash n-gram embedding 语义召回
fallback_like_candidates：SQL LIKE 兜底检索，类似关键词粗匹配，不是直接 grep 原文件
真正按 chunk 数量变化的是 semantic_candidates() 里面的策略，在 [scripts/query_knowledge_base.py (line 759)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:759)：
chunk < 10000:
  直接扫描 chunks.embedding，逐个算 cosine

chunk >= 10000:
  先用 embedding_lsh 找候选，再算 cosine
  如果 LSH 候选太少，再回退到全量 embedding 扫描
fallback_like_candidates() 也有类似变化：
chunk < 10000:
  读取 chunks 后在 Python 里算 sentence_score

chunk >= 10000:
  用 SQL LIKE 先筛选，避免全量 Python 扫描太慢

### 所以答辩时建议这么讲：

本项目始终采用混合检索，不是低 chunk 只用 FTS、高 chunk 才混合。低 chunk 场景下，FTS5、向量相似度和 LIKE 兜底都会参与，只是向量召回采用 SQLite 全量扫描计算 cosine；高 chunk 场景下，仍然是混合检索，但语义召回会先通过 embedding_lsh 做候选粗筛，提高性能。
如果你之前记成“低 chunk 用 FTS/GREP，高 chunk 用混合”，更像是一个简化说法；按当前代码实现，应改成：
小规模：FTS5 + 向量全量扫描 + LIKE 兜底 + rerank
大规模：FTS5 + LSH 向量候选召回 + SQL LIKE 兜底 + rerank

## hash n-gram embedding 哪里使用了

**在下面：小规模：FTS5 + 向量全量扫描 + LIKE 兜底 + rerank**
大规模：FTS5 + LSH 向量候选召回 + SQL LIKE 兜底 + rerank 条件下那个地方使用到了

对，就是这个位置用到了：

```
小规模：FTS5 + 向量全量扫描 + LIKE 兜底 + rerank
             ↑
        hash n-gram embedding 在这里用

大规模：FTS5 + LSH 向量候选召回 + SQL LIKE 兜底 + rerank
             ↑
        hash n-gram embedding 也在这里用
```

更准确地拆开讲：

1. 入库时用一次
   每个 chunk 会生成 `hash n-gram embedding`，存到 `chunks.embedding`。

位置：[scripts/ingest_knowledge_base.py (line 1560)](/D:/agent/local-knowledge/scripts/ingest_knowledge_base.py:1560)

```
embedding_values = local_embedding(chunk)
```

1. 查询时用一次
   用户问题也会生成同样的 embedding。

位置：[scripts/query_knowledge_base.py (line 765)](/D:/agent/local-knowledge/scripts/query_knowledge_base.py:765)

```
query_embedding = local_embedding(question)
```

1. 小规模时
   直接拿“问题 embedding”和所有“chunk embedding”逐个算余弦相似度，得到 `semantic_score`。
2. 大规模时
   先用问题 embedding 生成 LSH bucket，再去 `embedding_lsh` 表里找同 bucket 的 chunk，缩小候选范围，然后再算余弦相似度。
3. rerank 时
   如果某个 chunk 是 FTS5 或 LIKE 召回来的，系统还会补算它和问题的向量相似度，把它加入最终重排分数。

### 所以一句话答辩版：

`hash n-gram embedding` 用在语义召回链路中。小规模时用于全量向量相似度扫描；大规模时用于生成 LSH bucket 做候选粗筛；同时在 rerank 阶段为所有候选补充 `semantic_score`。





# 精炼信息：

## 分块embedding得逻辑是什么？  --> 项目维度为384 

这个项目的“分块 + embedding”是纯本地逻辑，不调用外部 embedding 模型

每个 chunk 生成一个 `chunk_id`，写入 `chunks` 表，同时写入 FTS5 表和 LSH 辅助表。



## 文件处理：

- `.md/.txt`：直接读文本。
- `.docx`：解压 Word XML，附带图片 OCR。
- `.doc`：先用 Word 转 `.docx`，再处理。
- `.pdf`：多种文本抽取方式 + 表格抽取 + 必要时 OCR。
- `.xlsx`：解压 Excel XML，转成工作表/行文本，再额外支持部分结构化表格解析。



## embedding_lsh是什么意思：

**`embedding_lsh` 不是最终答案，也不是新的 embedding 模型；它只是一个索引表，用来在资料很多时更快找到可能相似的分块。**

当前代码里，只有当 chunk 数量达到 `10000` 以上时，才优先使用这个 LSH 候选筛选；小知识库会直接扫描全部 embedding。



## hash n-gram embedding n如何解释

本项目采用多粒度 n-gram，兼顾短术语召回和长术语精确匹配；最终值不是拍脑袋，而是通过问答命中率、误召回率、Top-K 结果相关性和响应耗时来验证。

补一句：这套实现里 `n` 目前是代码硬编码的 `(2, 3, 4)`，不是配置文件动态读取。



### 部分举例说明：

#### chunk_size介绍

这两句话可以拆开理解。

```
CHUNK_MAX_CHARS = 1100
CHUNK_MIN_CHARS = 180
```

这两个值的意思是：

`CHUNK_MAX_CHARS = 1100`：普通情况下，一个 chunk 尽量不要超过 1100 个字符。它不是“每块固定 1100 字”，而是“合并语义单元时的上限”。如果一段太长，会再按换行、句号、中文句末符号等位置拆。

`CHUNK_MIN_CHARS = 180`：不是说每个 chunk 一定至少 180 字，而是说“只有合并后的候选文本超过 180 字时，才允许因为标题、语义相似度低等原因断块”。它用来避免切出很多很短的碎片。

举例：

```
当前块：120 字
下一个 unit：标题，30 字
合并后：150 字
```

即使下一个 unit 是标题，也通常不会断，因为 `150 <= 180`。

再比如：

```
当前块：160 字
下一个 unit：新标题，40 字
合并后：200 字
```

这时超过了 180，并且新 unit 是标题，就会断块：前面的 160 字作为一个 chunk，新标题开始下一个 chunk。

所以 180 更准确地说是“允许断块的门槛”，不是 chunk 的绝对最小长度。短文、最后一块、标题块仍然可能小于 180。

embedding 这段可以理解成：把文本里的 term 投影到一个 384 维的本地向量空间里。

比如文本：

```
本地知识库 embedding 384
```

会提取这些 term：

```
embedding
384
本地 地知 知识 识库
本地知 地知识 知识库
本地知识 地知识库
```

每个 term 会这样处理：

1. 用 `blake2b` 算一个稳定 hash。
2. `hash % 384` 得到维度编号，范围是 `0..383`。
3. 从 hash 的某一位决定这个 term 加正数还是负数。
4. 根据 term 长度给权重：`1.0 + min(len(term), 8) / 8.0`。

例子：

```
embedding -> index 235, sign -, weight 2.0
384       -> index 135, sign -, weight 1.375
知识      -> index 4,   sign -, weight 1.25
知识库    -> index 210, sign -, weight 1.375
本地知识  -> index 211, sign +, weight 1.5
```

然后把落到同一个 index 的值累加，最后做 L2 normalize。上面这句话生成的归一化稀疏向量类似：

```
[
  [4, -0.264628],
  [19, -0.264628],
  [82, 0.291091],
  [84, -0.317554],
  [135, -0.291091],
  [180, 0.264628],
  [210, -0.291091],
  [211, 0.317554],
  [222, 0.264628],
  [235, -0.423405],
  [285, -0.291091]
]
```

`[235, -0.423405]` 的意思是：这个文本在第 235 维上的值是 `-0.423405`。没出现的维度默认是 0，所以不用存满 384 个数字，这就是“稀疏向量 JSON”。

查询时，问题也用同样方法转成 384 维本地向量，再和 chunk 的向量算余弦相似度。相似度越高，说明它们共享的英文 token、数字 token、中文 ngram 模式越接近。

#### 用 blake2b 算一个稳定 hash。  从 hash 的某一位决定这个 term 加正数还是负数这两句话怎么理解

`blake2b` 是一个哈希函数。这里它的作用不是加密，而是把任意 term 稳定地变成一个很大的整数。

比如：

```
term = "知识库"
```

代码会做：

```
digest = hashlib.blake2b("知识库".encode("utf-8"), digest_size=8).digest()
value = int.from_bytes(digest, "big")
```

得到一个稳定整数：

```
16527372563249087058
```

“稳定”的意思是：只要 term 还是 `"知识库"`，每次算出来都是这个值。重启程序、换时间、重新导入，结果都一样。

然后：

```
index = value % 384
```

得到：

```
16527372563249087058 % 384 = 210
```

所以 `"知识库"` 被放到第 `210` 维。

“从 hash 的某一位决定正负号”指的是这行：

```
sign = 1.0 if (value >> 8) & 1 else -1.0
```

可以这样理解：

1. `value` 这个大整数在计算机里本质是一串二进制位。
2. `value >> 8` 表示把它右移 8 位。
3. `& 1` 表示只看最后一位是 `0` 还是 `1`。
4. 如果这一位是 `1`，这个 term 加正数；如果是 `0`，加负数。

例如 `"知识库"`：

```
value = 16527372563249087058
index = 210
sign = -1
weight = 1.375
```

所以它会让第 210 维加上：

```
-1.375
```

再比如 `"本地知识"`：

```
value = 2378144925508374355
index = 211
sign = +1
weight = 1.5
```

它会让第 211 维加上：

```
+1.5
```

为什么还要分正负？因为很多不同 term 会被压到 384 个维度里，肯定会有碰撞。正负号可以降低碰撞带来的偏差：有些 term 撞到同一维会相加，有些会相互抵消，不至于所有碰撞都只往正方向堆。

#### 对于一段长的中文字，那它提取出来的term是什么呢

对于一段连续中文，它不是按“词典分词”，也不是按整句，而是按 **2 字、3 字、4 字滑动窗口 ngram** 提取 term。

代码逻辑等价于：

```
for block in re.findall(r"[\u4e00-\u9fff]+", text):
    for size in (2, 3, 4):
        terms.extend(block[i : i + size] for i in range(len(block) - size + 1))
```

比如文本：

```
本地知识库支持离线检索
```

这是一段连续中文 block，会提取：

2 字 term：

```
本地 地知 知识 识库 库支 支持 持离 离线 线检 检索
```

3 字 term：

```
本地知 地知识 知识库 识库支 库支持 支持离 持离线 离线检 线检索
```

4 字 term：

```
本地知识 地知识库 知识库支 识库支持 库支持离 支持离线 持离线检 离线检索
```

所以它不理解“本地知识库”是一个完整词，也不理解“离线检索”是一个词。它只是机械地提取相邻 2、3、4 个中文字符组合。

如果中间有标点，会被拆成不同 block。例如：

```
本地知识库，支持离线检索。
```

中文 block 是两个：

```
本地知识库
支持离线检索
```

分别提取 ngram，不会跨过逗号生成：

```
识库支
知识库支
```

这种 term。

对于长度为 `n` 的连续中文段：

- 2 字 term 数量：`n - 1`
- 3 字 term 数量：`n - 2`
- 4 字 term 数量：`n - 3`

如果 `n >= 4`，总数是：

```
(n - 1) + (n - 2) + (n - 3) = 3n - 6
```

比如 100 个连续中文字符，会提取大约 `294` 个中文 term。重复 term 不会去重，会重复进入向量计算，重复越多，对应维度累加权重越大。

有些 term 撞到同一维会相加，有些会相互抵消，不至于所有碰撞都只往正方向堆 这句话怎么理解

因为这个项目把无限多的 term 压进固定的 `384` 个维度里，所以一定会出现这种情况：

```
term A -> 第 210 维
term B -> 第 210 维
```

这就叫“碰撞”：两个不同 term 被映射到了同一个维度。

如果没有正负号，所有 term 都只会往这个维度加正数：

```
term A: +1.25
term B: +1.50

第 210 维 = 1.25 + 1.50 = 2.75
```

问题是：`term A` 和 `term B` 可能语义完全无关，但因为碰巧撞到同一维，它们会被错误地看成“共同增强了同一个特征”。

所以代码引入正负号：

```
term A -> 第 210 维，+1.25
term B -> 第 210 维，-1.50
```

这时第 210 维变成：

```
1.25 - 1.50 = -0.25
```

它们的影响互相抵消了一部分。

再举一个检索相似度的例子。

假设文档 chunk 里有：

```
知识库
```

查询里有一个完全无关的词：

```
香蕉皮
```

但它们刚好都 hash 到第 `210` 维。

如果都只加正数：

```
文档第 210 维：+1.3
查询第 210 维：+1.4
```

算余弦相似度时，这一维会产生正贡献，让系统误以为它们有点相似。

如果有正负号，可能变成：

```
文档第 210 维：+1.3
查询第 210 维：-1.4
```

这一维就不会错误增强相似度，甚至会给负贡献。

当然，正负号不能消除所有碰撞。有时两个无关 term 也可能同维同号，还是会误加；但整体统计上，一半正、一半负，碰撞误差会更接近随机噪声，而不是全部偏向“越来越相似”。



## 项目里的向量数据库体现在哪里

本项目没有引入独立向量数据库，而是采用轻量化本地向量索引方案：用 SQLite 作为统一存储，chunks.embedding 保存分块向量，embedding_lsh 作为向量候选召回索引，再结合 FTS5 全文检索和关键词重排完成混合检索。对于当前本地知识库规模，这种方案部署简单、离线可用、维护成本低；当数据量达到几万 chunk 以上时，可以迁移到 FAISS、Qdrant、Milvus 或 sqlite-vec。



## 重排和召回及rerank怎么操作的呢？

本项目没有依赖独立向量数据库，而是采用本地轻量混合检索。召回阶段同时使用 FTS5 全文召回、本地 hash n-gram embedding 语义召回和 LIKE 兜底召回；重排阶段将语义相似度、关键词匹配度、全文检索分和文本质量分融合，得到最终 rerank_score。这种方式适合本地知识库和中小规模资料，部署简单、离线可用；数据量更大时可以平滑替换为 Qdrant、Milvus、FAISS 等专业向量库。



## 这里的semantic_score

  semantic_score 解决“意思像不像”，keyword_score 解决“关键词有没有命中”，fts_score 解决“全文检索相关性”，information_quality_score 解决“这段内容有没有足够信息量”。最后系统把四者融合成 rerank_score，所以不是单纯靠向量，也不是单纯靠关键词，而是混合重排。



## 帮我列举出完整的查询流程

 本项目的查询流程是：用户问题进入系统后，先读取运行配置和缓存，再判断本地索引状态；如果是缩写、站位、Excel 等业务问题，优先走结构化检索器；否则进入通用混合检索，通过 FTS5 全文召回、本地 hash n-gram embedding 语义召回、SQL LIKE 兜底召回三路获取候选 chunk，再用语义分、关键词分、BM25 分和内容质量分进行 rerank；随后扩展上下文、过滤低相关结果、生成 sources 和 citations，必要时融合联网证据，最后通过本地模板或 API grounded summary 生成带引用的中文答案，并写入缓存返回。



## 我记得不是低chunk用FTS、grep检索嘛？ 高chunk用混合检索，现在是全部都用混合检索嘛？

本项目始终采用混合检索，不是低 chunk 只用 FTS、高 chunk 才混合。低 chunk 场景下，FTS5、向量相似度和 LIKE 兜底都会参与，只是向量召回采用 SQLite 全量扫描计算 cosine；高 chunk 场景下，仍然是混合检索，但语义召回会先通过 embedding_lsh 做候选粗筛，提高性能。
如果你之前记成“低 chunk 用 FTS/GREP，高 chunk 用混合”，更像是一个简化说法；按当前代码实现，应改成：
小规模：FTS5 + 向量全量扫描 + LIKE 兜底 + rerank
大规模：FTS5 + LSH 向量候选召回 + SQL LIKE 兜底 + rerank



## hash n-gram embedding 哪里使用了

`hash n-gram embedding` 用在语义召回链路中。小规模时用于全量向量相似度扫描；大规模时用于生成 LSH bucket 做候选粗筛；同时在 rerank 阶段为所有候选补充 `semantic_score`。



## source_fingerprint与content_hash

`source_fingerprint` 判断“这个原始文件需不需要重新处理”；`content_hash` 判断“这个文件抽出来的内容是不是已经被别的文件入库过



## FAQ降级策略是怎么做的

假设 `knowledge.db` 被删除了，但 `faq.jsonl` 还在。用户问：`SWDL2`。系统先发现索引不存在，于是进入 FAQ 兜底；如果 FAQ 中有包含 `SWDL2` 的条目，就返回 `faq_fallback`，并附上原文件和 `chunk_id` 引用。若用户加了 `--web`，还会把 FAQ 证据和网页证据合并成混合结果。

反过来，如果索引是正常的，只是问题没搜到答案，系统不会走 FAQ；它会返回“本地知识库没有找到足够依据”，或在允许联网时走联网兜底。这一点是为了避免 FAQ 覆盖正常检索质量判断。



