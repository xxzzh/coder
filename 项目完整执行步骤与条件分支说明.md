# Local Knowledge Base Agent 项目完整执行步骤与条件分支说明

本文档用于说明当前项目从安装、配置、导入资料、索引、查询、联网、API 精炼、Web UI、健康检查到打包发布的完整执行路径。目标是让使用者和维护者能够判断每一步实际做了什么、在不同条件下会走哪条分支、出现异常时应该检查哪里。

## 1. 项目定位

本项目是一个本地知识库问答 Agent。用户把资料放入 `knowledge_base/raw` 后，系统会把资料抽取成文本、写入 SQLite FTS5 索引和本地 hash n-gram embedding，然后按“本地优先”的规则回答问题。

核心规则：

- 本地资料永远优先。
- 本地查到足够依据时，不联网搜索。
- 本地查不到且允许联网时，才联网搜索补充。
- 有 API key 且本次允许 API 时，API 只基于本地或联网引用做中文总结，不允许脱离引用自由回答。
- 所有命令行查询输出都是 JSON 结构化结果。
- Web UI 展示的是同一套查询结果，只是把 JSON 渲染成页面。

## 2. 关键目录和文件

| 路径 | 作用 |
|---|---|
| `start.bat` | 普通用户菜单入口。 |
| `run-agent.bat` | 命令行统一入口。 |
| `README.md` | 面向用户的快速说明。 |
| `requirements.txt` | 可选增强依赖。核心命令行依赖标准库即可运行。 |
| `.env` | API 配置文件，保存 `LKA_*` 配置。 |
| `knowledge_base/raw` | 原始资料目录，只放知识源文件。 |
| `knowledge_base/index/knowledge.db` | SQLite 本地索引数据库。 |
| `knowledge_base/processed` | 抽取报告、分块、FAQ、质量报告、复核批准记录。 |
| `scripts/ingest_knowledge_base.py` | 资料抽取和索引构建。 |
| `scripts/manage_knowledge_base.py` | update、rebuild、sources 管理命令。 |
| `scripts/query_knowledge_base.py` | 查询主逻辑。 |
| `scripts/web_agent.py` | 本地 Web UI 和 HTTP API。 |
| `scripts/healthcheck_agent.py` | 健康检查。 |
| `scripts/configure_api.py` | API 提供商、模型发现和验证。 |
| `scripts/api_providers.py` | OpenAI-compatible API 调用封装。 |
| `scripts/retrievers` | 领域专用结构化检索器。 |
| `scripts/test_retrieval_guardrails.py` | 离线回归测试。 |
| `scripts/package_dist.py` | 构建 `dist/local-knowledge` 和 zip 包。 |

## 3. 支持的资料格式

| 格式 | 抽取方式 |
|---|---|
| `.md`、`.txt` | 纯文本读取。 |
| `.docx` | 读取 Word XML；如有嵌入图片会尝试 OCR。 |
| `.doc` | 通过本机 Microsoft Word COM 转成临时 `.docx` 再抽取。 |
| `.xlsx` | 读取 workbook、worksheet、sharedStrings；如有图片会尝试 OCR；图表和绘图会触发复核提示。 |
| `.pdf` | 优先 `pdfplumber`、`pypdf/PyPDF2`、`pdftotext`；必要时尝试 OCR；最后才退回 PDF 流字面量抽取。 |

不支持的文件类型不会入库，健康检查会在 `raw_unsupported_files` 中列出。

## 4. 普通用户完整使用步骤

### 4.1 首次使用

1. 双击 `start.bat`。
2. 选择 `1. First-time setup / configure API`。
3. 如果需要 API 精炼，按提示配置提供商、API key 和模型；如果不配置，仍可纯本地查询。
4. 选择 `3. Open raw materials folder`，把资料放入 `knowledge_base/raw`。
5. 选择 `4. Strict rebuild index`，严格重建索引。
6. 选择 `6. Health check`，确认输出中 `"ok": true`。
7. 选择 `2. Start Web UI`，打开浏览器页面。
8. 在 Web UI 输入问题并查询。

### 4.2 后续新增或修改资料

1. 把新增或修改后的文件放入 `knowledge_base/raw`。
2. 少量变化优先选择 `5. Incremental update index`。
3. 如果出现抽取异常、重复、缓存异常或想完全刷新，选择 `4. Strict rebuild index`。
4. 再运行 `6. Health check`，确认 `"ok": true`。

### 4.3 命令行等价步骤

```bat
.\run-agent.bat setup
.\run-agent.bat rebuild-strict
.\run-agent.bat verify
.\run-agent.bat web --open
```

直接查询：

```bat
.\run-agent.bat "hpd test"
```

只查本地，不联网：

```bat
.\run-agent.bat ask "hpd test" --no-web
```

显式允许联网：

```bat
python scripts\query_knowledge_base.py "雷军是谁" --web
```

禁用 API，只看本地或联网引用整理结果：

```bat
python scripts\query_knowledge_base.py "hpd test" --web --no-api
```

## 5. start.bat 菜单对应动作

| 菜单项 | 实际动作 |
|---|---|
| `1` | 调用 `run-agent.bat setup`，创建目录、检查 FTS5、可选配置 API、可选更新索引。 |
| `2` | 调用 `run-agent.bat web --open`，启动 `http://127.0.0.1:8765/`。 |
| `3` | 打开 `knowledge_base/raw`。 |
| `4` | 调用 `run-agent.bat rebuild-strict`，严格重建索引。 |
| `5` | 调用 `run-agent.bat update`，增量更新索引。 |
| `6` | 调用 `run-agent.bat verify`。 |
| `7` | 读取用户输入并调用 `run-agent.bat "问题"`，默认允许联网兜底。 |
| `8` | 调用 `run-agent.bat sources`。 |
| `9` | 执行 OCR 测试材料评测。 |
| `10` | 生成 OCR 测试材料。 |
| `11` | 安装离线英文到中文翻译模型。 |
| `12` | 配置答案精炼 API。 |

## 6. run-agent.bat 命令对应动作

| 命令 | 实际动作 |
|---|---|
| `setup` | 执行 `scripts/setup_agent.py`。 |
| `verify` / `healthcheck` | 执行 `scripts/healthcheck_agent.py`。 |
| `web` | 执行 `scripts/web_agent.py`。 |
| `translation-setup` | 执行 `scripts/install_translation_model.py`。 |
| `api-setup` | 执行 `scripts/configure_api.py`。 |
| `ingest` | 执行 `scripts/ingest_knowledge_base.py knowledge_base/raw`。 |
| `update` | 执行 `scripts/manage_knowledge_base.py update knowledge_base/raw`。 |
| `sources` | 执行 `scripts/manage_knowledge_base.py sources`。 |
| `rebuild` | 执行非严格重建。 |
| `rebuild-strict` | 执行严格重建，待复核文件会跳过。 |
| `ask "问题" --no-web` | 只查本地。 |
| `ask "问题"` | 本地优先，本地不足时允许联网。 |
| 直接输入问题 | 本地优先，本地不足时允许联网。 |

注意：`run-agent.bat "问题"` 默认会传 `--web`；`python scripts\query_knowledge_base.py "问题"` 默认不联网，除非显式加 `--web`。

## 7. 索引构建流程

索引入口是 `scripts/manage_knowledge_base.py`，底层调用 `scripts/ingest_knowledge_base.py`。

### 7.1 增量更新 update

执行：

```bat
.\run-agent.bat update
```

流程：

1. 确保 `knowledge_base/raw` 和 `knowledge_base/processed` 存在。
2. 打开或创建 `knowledge_base/index/knowledge.db`。
3. 遍历 `raw` 下所有支持格式文件。
4. 忽略 Office 临时锁文件，如 `~$xxx.docx`。
5. 计算每个文件的 `source_fingerprint`。
6. 如果文件 fingerprint 没变化，则跳过该文件。
7. 如果有变化，则重新抽取文本、分块、写入索引。
8. 删除 raw 中已不存在的旧文档索引。
9. 写入 `metadata.source_fingerprint`。
10. 写入 `documents.jsonl`、`chunks.jsonl`、`faq.jsonl`、`extraction_report.jsonl`、`retrieval_quality_report.json`。
11. 如有文档变化或删除，清空查询缓存 `query_cache`。

### 7.2 严格重建 rebuild-strict

执行：

```bat
.\run-agent.bat rebuild-strict
```

流程：

1. 删除生成文件：`knowledge.db`、`documents.jsonl`、`chunks.jsonl`、`faq.jsonl`、`extraction_report.jsonl`、`retrieval_quality_report.json`。
2. 从 `raw` 重新导入全部资料。
3. 如果某文件 `requires_review=true` 且没有批准记录，则严格模式跳过该文件。
4. 如果文件已在 `review_approvals.json` 中批准且 fingerprint 匹配，则允许入库。

适用场景：

- 首次建库。
- 抽取逻辑发生变化。
- 索引异常。
- 查询结果明显不符合新资料。
- 需要清理旧缓存或旧结构。

### 7.3 非严格重建 rebuild

执行：

```bat
.\run-agent.bat rebuild
```

非严格重建也会删除旧索引再导入，但不会因为 `requires_review=true` 直接跳过文件。普通用户更推荐使用 `rebuild-strict`，避免把疑似抽取不完整的资料静默入库。

## 8. 资料抽取和复核规则

每个文件都会产生抽取报告，位置：

```text
knowledge_base/processed/extraction_report.jsonl
```

关键字段：

| 字段 | 含义 |
|---|---|
| `file_path` | raw 内相对路径。 |
| `file_type` | 文件类型。 |
| `method` | 实际使用的抽取方式。 |
| `text_chars` | 抽取出的文本字符数。 |
| `warnings` | 抽取风险提示。 |
| `requires_review` | 是否需要人工复核。 |
| `review_approved` | 是否已经批准。 |

会触发复核的典型情况：

- PDF 或 Office 文件抽取文本过短。
- PDF 有图片但 OCR 没得到文本。
- DOCX/XLSX 有嵌入图片但 OCR 没得到文本。
- XLSX 有 chart/drawing，可能存在只在图表里的文字。

批准记录位置：

```text
knowledge_base/processed/review_approvals.json
```

Web UI 可通过“待复核资料”面板批准；批准后会自动触发严格增量更新。批准记录按文件 fingerprint 绑定，文件内容变更后必须重新复核。

## 9. 索引数据库内容

SQLite 数据库位置：

```text
knowledge_base/index/knowledge.db
```

主要表：

| 表 | 作用 |
|---|---|
| `metadata` | 索引时间、embedding 模型、chunk 策略、source fingerprint。 |
| `documents` | 文档级信息和完整抽取文本。 |
| `chunks` | 分块文本和本地 embedding。 |
| `chunks_fts` | SQLite FTS5 全文索引。 |
| `embedding_lsh` | 大规模 chunk 时的 embedding 候选召回辅助表。 |
| `query_cache` | 查询缓存。 |

## 10. 查询执行流程

查询入口：

```bat
python scripts\query_knowledge_base.py "问题" [--web] [--no-api]
```

核心流程：

1. 读取 `.env` 和环境变量，得到 API 配置。
2. 如果 `knowledge.db` 不存在，尝试 `faq.jsonl` 兜底。
3. 如果索引存在，检查 raw 是否变化。
4. 如果 raw 变化，后台启动异步更新，当前查询仍先使用现有索引。
5. 检查查询缓存。
6. 运行领域专用结构化检索器。
7. 运行通用检索：FTS5、semantic、LIKE fallback。
8. rerank 后判断本地结果是否足够相关。
9. 本地相关则返回本地证据。
10. 本地不相关且未允许联网，则返回 `source_type=none`。
11. 本地不相关且允许联网，则搜索网页、抓取公开网页正文、整理证据。
12. 最终答案进入 `finalize_answer`：可调用 API 精炼，也可本地生成中文 fallback。
13. 输出 JSON。

## 11. 查询结果字段说明

常见字段：

| 字段 | 含义 |
|---|---|
| `answer` | 最终答案。 |
| `source_type` | 来源类型。 |
| `sources` | 证据来源列表。 |
| `citations` | 用于 API 或本地总结的引用。 |
| `need_web_search` | 是否仍需要联网才能补足。 |
| `web_search_used` | 本次是否实际联网搜索。 |
| `api_used` | 本次是否实际调用 API。 |
| `retrieval_mode` | 检索模式。 |
| `answer_mode` | 答案生成模式。 |
| `cache_hit` | 是否命中缓存。 |
| `index_status` | `ready` 或 `updating`。 |
| `query.original` | 原始问题。 |
| `query.normalized` | 归一化问题。 |
| `query.canonical` | 缓存和等价问法使用的 canonical key。 |
| `query.web_query` | 联网搜索关键词。 |

常见 `source_type`：

| 值 | 含义 |
|---|---|
| `knowledge_base` | 本地知识库命中。 |
| `web_search` | 本地不足后联网命中。 |
| `hybrid_web_knowledge` | 本地和联网证据合并。 |
| `faq_fallback` | 主索引不可用时使用 FAQ 兜底。 |
| `none` | 没有足够依据，且未联网或联网不可用。 |

## 12. 本地检索规则

本地检索由三类候选组成：

1. `fts_candidates`：SQLite FTS5 全文召回。
2. `semantic_candidates`：本地 hash n-gram embedding 语义召回。
3. `fallback_like_candidates`：LIKE 兜底召回。

候选会进入 rerank：

- semantic score
- keyword / term score
- FTS score
- chunk information quality

如果 top row 分数低于阈值，或 query terms 覆盖不足，则判定本地没有足够依据。

## 13. 领域专用结构化检索

查询通用检索前，会先走 `scripts/retrievers`：

| retriever | 作用 |
|---|---|
| `abbreviation` | 缩写词查询，如 `PR是什么`、`GW`。 |
| `station_test` | 站位/测试项查询，如 `FFT测试项`、`FAT有哪些测试项`。 |
| `excel_table` | 特定 Excel 表格结构化问答。 |
| `general_rag` | 通用 RAG 扩展入口。 |

结构化结果通常直接返回，`answer_mode` 以 `local_structured_` 开头，并且不会调用 API。

例子：

- `雷军是谁` 和 `雷军是什么` 的联网搜索关键词会归一为 `雷军`。
- `FFT测试`、`FFT测试项`、`人工功能测试` 会归一到同一类测试项问题。
- `FRT站位有哪些测试项` 和 `FRT有哪些测试项` 使用同一类 station canonical key。

## 14. 联网搜索规则

联网只在 `use_web=true` 且本地没有足够依据时发生。

会尝试的搜索源包括：

- 百度相关入口
- Bing China
- Bing RSS
- DuckDuckGo
- Wikipedia
- Jina Bing

联网流程：

1. 生成 `web_query`。
2. 并行或按 provider 查询搜索结果。
3. 过滤低相关、低质量、噪声结果。
4. 去重。
5. 抓取前几个公开网页正文。
6. 复用本地分块和 rerank 逻辑抽取相关网页证据。
7. 形成 `web_search` 或 `hybrid_web_knowledge` 结果。

联网失败分支：

| 情况 | 返回含义 |
|---|---|
| 搜索源都不可访问 | `web_search_reason=providers_unavailable`。 |
| 搜索源可访问但没结果 | `web_search_reason=no_search_results`。 |
| 有结果但不够相关 | `web_search_reason=no_relevant_results`。 |
| 网页抓取被拒绝 | 保留搜索摘要，记录 `page_fetch_error`。 |

## 15. API 精炼规则

API 配置来自 `.env`：

```env
LKA_USE_API=true
LKA_API_PROVIDER=deepseek
LKA_API_BASE_URL=https://api.deepseek.com
LKA_API_KEY=...
LKA_API_MODEL=deepseek-v4-flash
LKA_API_TIMEOUT_SECONDS=20
```

支持提供商：

- 小米 MiMo
- DeepSeek
- OpenAI
- 自定义 OpenAI 兼容接口

API 只在以下条件全部满足时调用：

1. `allow_api=true`，即命令行没有 `--no-api`，Web UI 请求也允许 API。
2. `.env` 中 `LKA_USE_API=true`。
3. `LKA_API_KEY` 非空。
4. `LKA_API_MODEL` 非空。
5. API key 和 base URL 未被规则拒绝。
6. 当前结果有可用 citations。
7. 当前 `source_type` 是 `knowledge_base`、`web_search`、`hybrid_web_knowledge` 或 `faq_fallback`。
8. 不是本地结构化答案。

API 的 system prompt 明确要求：

- 只根据给定 citations 回答。
- 始终用简体中文回答。
- 不得编造。
- 引用 source id。
- 证据不足时说明证据不足。

API 异常时：

- 记录 `api_error`。
- 降级为本地中文 fallback。
- `api_used=false`。

MiMo Token Plan 的 `tp-` key 会被拒绝作为后端应用 API。

## 16. 离线翻译和无 API 情况

如果没有 API 或本次使用 `--no-api`：

- 本地结构化答案直接返回中文结构化答案。
- 中文资料通常直接基于引用生成中文答案。
- 英文资料会尝试使用 Argos Translate 离线英译中。
- 如果没有安装离线翻译模型，则返回中文提示，并保留原文引用来源。

安装离线翻译：

```bat
python -m pip install -r requirements.txt
.\run-agent.bat translation-setup
```

## 17. 条件分支矩阵

| 条件 | 行为 |
|---|---|
| 无索引，且无 FAQ | 返回索引不存在；Web UI 在答案框下方用红色提示当前没有与问题对象相关的本地索引和 FAQ，并基于问题生成建议问法。`SWDL2` 只是示例，其他类似对象也会按同一规则生成提示，例如小写 `swdl2` 会提示“你是否想问：SWDL2有哪些测试项？”，`hpd test` 会提示“你是否想问：HPD有哪些测试项？”，中文问题则至少提示换成“对象是什么”这类更完整的问题。允许联网时可继续走联网搜索。 |
| 有索引，但当前问法无足够命中 | 返回“本地知识库没有找到足够依据”；Web UI 在答案框下方用红色提示当前问法没有命中与问题对象相关的足够依据，并给出更完整的建议问法。有索引时具体建议问法必须先经过本地检索预检，不能凭对象名猜测。例如 `我的个人密码是` 会提示“你是否想问：个人密码是什么？”。如果 `SR` 在缩写词典中有答案，则提示 `SR是什么`，不会误提示 `SR有哪些测试项`。如果对象完全不存在，例如 `ZZZ999有哪些测试项`，则只提示换成更完整问法，不给出未验证的具体建议。 |
| 无索引，但有 FAQ 命中 | 返回 `faq_fallback`；如果允许联网可补充网页证据。 |
| 索引损坏 | 使用 FAQ 兜底；允许联网时再补充网页。 |
| raw 文件变化，索引还未更新 | 当前查询用旧索引；后台启动更新；结果带 `index_status=updating`。 |
| 本地结构化命中 | 直接返回本地结构化答案；不联网；不调用 API。 |
| 本地普通命中，API 可用 | API 基于本地 citations 精炼中文答案。 |
| 本地普通命中，API 不可用 | 本地中文 fallback 或离线翻译。 |
| 本地无命中，未允许联网 | 返回 `source_type=none`，提示需要 `--web`。 |
| 本地无命中，允许联网，无 API | 联网搜索并返回本地整理/翻译结果。 |
| 本地无命中，允许联网，有 API | 联网搜索后 API 基于网页 citations 总结。 |
| 联网搜索无可信结果 | 返回对应 `web_search_reason`，不会让 API 凭空回答。 |
| API key 不可用 | 降级本地 fallback。 |
| 查询重复且缓存成熟 | 可能返回 `cache_hit=true`。 |

## 18. 缓存规则

缓存表是 `query_cache`。

缓存 key 由以下内容组成：

- canonical question
- limit
- 是否允许联网
- cache variant
- API 配置相关变体

缓存 TTL：

- 正常结果：`3600` 秒。
- 空结果或无 sources 的负缓存：`120` 秒。

缓存命中还有热度门槛：命中次数低于 `HOT_QUERY_THRESHOLD` 时不会直接返回。索引发生文档变化或删除时会清空缓存。

## 19. Web UI 执行流程

启动：

```bat
.\run-agent.bat web --open
```

默认地址：

```text
http://127.0.0.1:8765/
```

主要 API：

| HTTP API | 作用 |
|---|---|
| `GET /` | 返回 Web 页面。 |
| `GET /api/health` | 健康状态。 |
| `GET /api/sources` | 索引来源。 |
| `GET /api/reviews` | 待复核资料。 |
| `GET /api/api-config` | 当前 API 配置摘要。 |
| `GET /api/query` | 非流式查询。 |
| `POST /api/query-stream` | 流式查询，页面主要使用它。 |
| `POST /api/update` | 严格增量更新。 |
| `POST /api/rebuild-strict` | 严格重建索引。 |
| `POST /api/reviews/approve` | 批准待复核文件并更新索引。 |
| `POST /api/open-raw-folder` | 打开资料目录。 |
| `POST /api/api-models` | 获取模型列表。 |
| `POST /api/api-save` | 保存并验证 API 配置。 |

页面上的 `online` 勾选框对应 `use_web=true`。

状态行示例：

```text
knowledge_base · api
web_search · bing_rss+wikipedia+baidu+bing_china · api
none · cache
```

含义：

- `knowledge_base`：本地命中。
- `web_search`：联网补充。
- provider 列表：实际搜索来源。
- `api`：使用了配置的 API 做引用总结。
- `cache`：结果来自缓存。
- `none`：没有足够依据。

## 20. 健康检查

执行：

```bat
.\run-agent.bat verify
```

健康检查通过条件：

- Python SQLite 支持 FTS5。
- `knowledge_base/raw` 存在。
- 无不支持的 raw 文件类型。
- 索引存在。
- documents 数量大于 0。
- chunks 数量大于 0。
- 待复核数量为 0。
- 索引没有 SQLite 错误。

重要字段：

| 字段 | 含义 |
|---|---|
| `ok` | 是否整体可用。 |
| `raw_supported_files` | 支持格式资料数量。 |
| `raw_unsupported_files` | 不支持格式清单。 |
| `index.documents` | 入库文档数。 |
| `index.chunks` | 入库分块数。 |
| `extraction_report.requires_review` | 待复核数量。 |
| `retrieval_quality_report.summary` | 检索质量摘要。 |
| `api_configured` | 是否填写 API 配置。 |
| `api_backend_usable` | API 后端是否允许使用。 |

## 21. 常见问题和处理

### 21.1 明明资料里有内容，但回答“没有找到足够依据”

按顺序检查：

1. 运行 `.\run-agent.bat verify`。
2. 确认 `raw_supported_files` 和 `index.documents` 数量正确。
3. 运行 `.\run-agent.bat sources`，确认对应文件已索引。
4. 检查 `knowledge_base/processed/extraction_report.jsonl`。
5. 如果 `requires_review>0`，在 Web UI 批准或处理抽取问题。
6. 对 PDF 尤其检查 `method` 是否是 `pdftotext`、`pdfplumber`、`pypdf_or_pypdf2` 或 `ocr_tesseract`，避免只有低质量 `pdf_stream_literals`。
7. 执行 `.\run-agent.bat rebuild-strict`。
8. 再查询。

### 21.2 Web UI 显示 `none · cache`

说明之前相同查询被缓存为空结果。处理方式：

- 等负缓存 TTL 过期。
- 或重建/更新索引，文档变化会清空缓存。
- 或换更具体的问法。

### 21.3 本地命中后为什么没有联网

这是设计要求。本地有足够依据时不会联网，以保证本地资料优先。

### 21.4 有 API key 为什么没有调用 API

常见原因：

- 命令行加了 `--no-api`。
- `.env` 中 `LKA_USE_API` 不是 `true`。
- API key 或 model 为空。
- 当前结果是本地结构化答案。
- 当前没有 citations。
- API key 类型被拒绝。
- API 调用失败并降级。

### 21.5 联网开启后为什么还是没有答案

可能原因：

- 搜索源不可访问。
- 搜索结果和问题不相关。
- 网页抓取被拒绝。
- 网络超时。
- API 不可用，但搜索也没有可用中文 fallback。

查看输出中的：

- `web_search_reason`
- `web_search_attempts`
- `page_fetch_status`
- `page_fetch_error`

## 22. 开发维护流程

开发维护操作均在当前项目目录执行：

```bat
cd /d <项目解压目录>
```

改代码后建议执行：

```bat
python scripts\test_retrieval_guardrails.py
python scripts\healthcheck_agent.py
```

如涉及索引抽取逻辑，再执行：

```bat
python scripts\manage_knowledge_base.py rebuild knowledge_base\raw --db knowledge_base\index\knowledge.db --processed knowledge_base\processed --strict-extraction
python scripts\healthcheck_agent.py
```

## 23. 打包发布流程

执行：

```bat
.\package-dist.bat
```

实际调用：

```bat
python scripts\package_dist.py
```

输出：

```text
dist/local-knowledge
dist/local-knowledge.zip
```

打包内容包括：

- 根目录用户入口文件。
- `AGENTS.md`。
- `audit-local.bat` 本地审计入口。
- `.codex` 项目级技能目录。
- `scripts`。
- `test_materials`。
- `tools`。
- 空的 `knowledge_base/raw` 子目录结构。

打包会排除：

- `__pycache__`
- `.pyc`
- `.pyo`
- `scripts/package_dist.py`

## 24. 推荐操作顺序

### 24.1 普通用户推荐顺序

```text
start.bat
  -> 1 首次配置
  -> 3 放资料
  -> 4 严格重建
  -> 6 健康检查
  -> 2 Web UI
  -> 查询
```

### 24.2 开发者推荐顺序

```text
读取代码和需求
修改代码
python scripts\test_retrieval_guardrails.py
必要时 rebuild-strict
python scripts\healthcheck_agent.py
```

### 24.3 资料问题排查顺序

```text
verify
sources
extraction_report.jsonl
reviews
rebuild-strict
query --no-api
query with API
```

## 25. 当前项目状态基准

截至本文档生成时，项目健康状态应满足：

- `raw_supported_files=11`
- `index.documents=11`
- `index.chunks=1797`
- `extraction_report.requires_review=0`
- `retrieval guardrails: ok`

如果这些数值后续因为资料变化而不同，不一定是异常；判断标准以 `healthcheck_agent.py` 输出的 `"ok": true` 和 `test_retrieval_guardrails.py` 输出 `retrieval guardrails: ok` 为准。
