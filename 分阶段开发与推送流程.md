# 分阶段开发与推送流程

本文档记录本项目采用的协作规则，也可复制到后续其他项目中复用。

## 核心原则

- 每个阶段只实现当前阶段目标，不提前进入下一阶段。
- 每个阶段完成后，必须给出当前阶段能做到什么。
- 每个阶段完成后，必须更新验证文档，写清楚验证命令和预期结果。
- 用户验证并回复 `ok` 或 `合格` 后，才提交并推送到 GitHub。
- 推送完成后，才继续进入下一阶段。

## 标准流程

### 1. 明确当前阶段目标

开始阶段前，先确认本阶段范围。

示例：

```text
当前只实现第二阶段：need_web_search 和 --web。
不会进入第三阶段。
```

### 2. 实现当前阶段

只修改当前阶段需要的文件。

如果发现前一阶段存在影响验收的问题，先修复该问题，不进入下一阶段。

### 3. 更新验证文档

每完成一个阶段，都要更新：

```text
阶段验证命令.md
```

验证文档应包含：

- 当前阶段能力
- 验证命令
- 预期结果
- 本阶段新增的输出要求或验收标准

### 4. 本地验证

开发者先在本地运行验证命令。

如果验证失败，继续修复，直到本地结果符合预期。

### 5. 等待用户验证

完成本地验证后，向用户说明：

- 本阶段完成了什么
- 修改了哪些关键文件
- 已运行哪些验证命令
- 用户需要运行哪些命令复核

然后停止，不进入下一阶段。

### 6. 用户确认后提交并推送

只有当用户明确回复：

```text
ok
```

或：

```text
合格
```

才执行 Git 提交和推送。

推荐提交格式：

```bat
git status --short
git add .
git commit -m "Complete phase N: short description"
git push
```

如果当前改动只是阶段验收修复，提交信息可以写：

```bat
git commit -m "Polish phase N output"
```

### 7. 进入下一阶段

推送成功后，再开始下一阶段。

进入下一阶段前应说明：

```text
第二阶段已验证并推送，现在开始第三阶段。
```

## GitHub 推送规则

- 不在用户验证前推送阶段代码。
- 推送前先检查状态：

```bat
git status --short --branch
```

- 如果远程有新提交，先检查并避免覆盖。
- 不使用强推，除非用户明确要求。
- 不提交生成缓存、索引文件或临时文件。

本项目已通过 `.gitignore` 排除：

```text
__pycache__/
*.py[cod]
knowledge_base/index/
knowledge_base/processed/
.env
.venv/
venv/
```

## 阶段验收模板

每个阶段结束时，可以按下面格式汇报：

```text
第 N 阶段已完成。

当前能做到：
- ...
- ...

关键修改：
- path/to/file
- path/to/file

已验证：
- command 1
- command 2

请你验证第 N 阶段。
如果合格，回复 ok 或 合格。
我收到确认后会提交并推送，然后再进入第 N+1 阶段。
```

## 本项目当前约定

本项目后续阶段按以下顺序推进：

1. 第一阶段：本地知识库 MVP
2. 第二阶段：增加“答不了就询问联网”
3. 第三阶段：增强准确性
4. 第四阶段：做成更方便的使用方式

当前已完成并推送：

- 第一阶段
- 第二阶段
- `README.md`

后续从第三阶段开始，继续执行本文档流程。
